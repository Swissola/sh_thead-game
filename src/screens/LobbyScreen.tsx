// Stub for Plan 01-05 Task 1's Router test — Task 3 replaces this with the
// full lobby extraction (room code, player list, host-only start) from App.tsx.
export function LobbyScreen() {
    return <div>Game Lobby</div>;
}
