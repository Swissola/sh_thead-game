---
phase: 03-responsive-ui
plan: 07
type: execute
wave: 4
depends_on: ['03-03', '03-04', '03-05']
files_modified:
  - src/screens/GameScreen.tsx
  - src/components/piles/DiscardPile.tsx
  - src/components/piles/DrawPile.tsx
  - src/components/piles/BurnPile.tsx
  - src/__tests__/screens/GameScreen.test.tsx
autonomous: true
requirements: [RESP-01, RESP-03]

must_haves:
  truths:
    - "The game board, the three piles, the table and the player tiles all fit a 375px-wide viewport with no horizontal overflow (RESP-01)"
    - "Below 640px the board genuinely rearranges rather than shrinking the desktop arrangement (D-09)"
    - "Every interactive control on the game screen is at least 44px in both dimensions (RESP-03)"
    - "One phone layout serves both portrait and landscape (D-12)"
  artifacts:
    - path: "src/screens/GameScreen.tsx"
      provides: "Phone-width board layout and touch-sized game controls"
      contains: "max-sm:"
    - path: "src/components/piles/DiscardPile.tsx"
      provides: "Discard pile that no longer forces a fixed 160px column at phone width"
  key_links:
    - from: "src/screens/GameScreen.tsx board grid"
      to: "DiscardPile / DrawPile / BurnPile"
      via: "a fixed-pixel three-column grid replaced by a wrapping phone layout"
      pattern: "grid-cols-\\[160px_100px_1fr\\]"
---

<objective>
Close RESP-01 for the game screen and finish RESP-03's control audit there: rearrange the board so nothing overflows a phone width, and grow the remaining undersized controls to 44px.

Purpose: the game screen currently pins its piles into a `grid-cols-[160px_100px_1fr]` track with a `gap-12`, sits that inside a `grid-cols-[auto_1fr] gap-8` alongside the table, and lays player tiles out in a hard `grid-cols-3`. That is roughly 400px of fixed columns plus 48px of gap before any content — it cannot fit a 375px phone. This is the last plan standing between the phase and its first success criterion.
Output: a board that reflows below `sm` and a game screen with no sub-44px control left.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-responsive-ui/03-CONTEXT.md
@.planning/phases/03-responsive-ui/03-RESEARCH.md
@.planning/phases/03-responsive-ui/03-PATTERNS.md
@.planning/phases/03-responsive-ui/03-UI-SPEC.md
@.planning/phases/03-responsive-ui/03-03-SUMMARY.md
@.planning/phases/03-responsive-ui/03-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reflow the board and the piles below the sm breakpoint</name>
  <files>src/screens/GameScreen.tsx, src/components/piles/DiscardPile.tsx, src/components/piles/DrawPile.tsx, src/components/piles/BurnPile.tsx, src/__tests__/screens/GameScreen.test.tsx</files>
  <read_first>
    - `src/screens/GameScreen.tsx` — the whole returned tree; specifically the `max-w-6xl mx-auto` page container, the header block (`bg-slate-800 rounded-xl p-4 mb-4`) with its logo/room/turn readout row, the board container (`bg-slate-800 rounded-xl p-6 mb-4`), the `grid grid-cols-[auto_1fr] gap-8 mb-4` wrapper holding `Table` and the piles, the inner `grid grid-cols-[160px_100px_1fr] gap-12 items-start` pile row, the Play / Pick Up Pile button row (`flex gap-3`), and the `grid grid-cols-3 gap-4` player-tile row
    - `src/components/piles/DiscardPile.tsx` — the `discard-pile-cards relative h-28` div carrying an inline `style={{ width: '160px', margin: '0 auto' }}`
    - `src/components/piles/DrawPile.tsx` and `src/components/piles/BurnPile.tsx` — both use `w-16 h-24` card slots plus a label and a count line
    - `.planning/phases/03-responsive-ui/03-CONTEXT.md` — D-09 (genuine rearrangement, not a scale-down), D-10 (stock breakpoints only), D-12 (one layout for both orientations)
    - `.planning/phases/03-responsive-ui/03-03-SUMMARY.md` — the hand strip already shipped, so the hand needs no further layout work here
    - `src/__tests__/screens/GameScreen.test.tsx` — the harness; note CLAUDE.md's warning that `.draw-card-ghost` and the `discard-pile-area`/`draw-pile-card` class names are queried by the draw animation and must survive
  </read_first>
  <action>
    Rearrange the board for phone width using only stock Tailwind breakpoints (D-10). The target arrangement below `sm` (640px) is: table and piles stacked vertically rather than side by side, the three piles wrapping into a compact cluster instead of a fixed three-column track, player tiles in a single column, and the action buttons full-width. Above `sm` every existing arrangement is preserved exactly.

    Concrete changes in `src/screens/GameScreen.tsx`:
    - `grid grid-cols-[auto_1fr] gap-8 mb-4` → add `max-sm:grid-cols-1 max-sm:gap-4` so `Table` and the pile cluster stack
    - `grid grid-cols-[160px_100px_1fr] gap-12 items-start` → add `max-sm:flex max-sm:flex-wrap max-sm:justify-center max-sm:gap-4` so the piles wrap into a cluster instead of holding three fixed-pixel tracks that alone exceed a 375px viewport
    - `grid grid-cols-3 gap-4` (player tiles) → add `max-sm:grid-cols-1` so each tile gets a full row rather than a third of 375px
    - board container `p-6` → `p-3 sm:p-6`, and the header container `p-4` → `p-3 sm:p-4`, recovering horizontal room at phone width
    - the header's `flex items-center justify-between` row and its `flex items-center gap-3` control cluster → add `max-sm:flex-wrap max-sm:gap-2` so the logo, the Rules/Leave buttons and the turn readout wrap rather than compress
    - the Play / Pick Up Pile row (`flex gap-3`) → add `max-sm:flex-col` so each button gets full width at phone size; both already use `flex-1`, which becomes full-width in a column

    In `src/components/piles/DiscardPile.tsx`, the inline `style={{ width: '160px', margin: '0 auto' }}` is the single hardest fixed-pixel constraint on the board. Replace the inline width with Tailwind classes on the same element: `w-40 mx-auto` at `sm` and above (`w-40` is `10rem` = 160px, identical to today) and `max-sm:w-32` below it. Keep the `discard-pile-cards` and `discard-pile-area` class names and the `relative h-28` sizing — the draw animation queries those selectors.

    In `DrawPile.tsx` and `BurnPile.tsx`, keep the `w-16 h-24` slots (they are already phone-sized) and keep the `draw-pile-card` class name. Only add `max-sm:` adjustments if the wrapping cluster demands them; do not restyle them speculatively.

    Do not add any custom breakpoint value, any `portrait:`/`landscape:` variant, or any global scale/zoom transform — D-09 rejects uniform scale-down and D-12 rejects orientation-specific layouts. Do not change the `Table` overlay's inline `marginTop: '-80px'` positioning in `src/components/Table.tsx`; that file belongs to plan 03-04 and touching it here would create a cross-plan conflict.

    Add class-level regression assertions to `src/__tests__/screens/GameScreen.test.tsx` for the reflow contract. jsdom does not apply Tailwind, so these are class-membership assertions; real overflow verification is plan 03-08's manual checkpoint.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/screens/GameScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/GameScreen.tsx` contains all of `max-sm:grid-cols-1`, `max-sm:flex-wrap`, `max-sm:flex-col`, `p-3 sm:p-6`
    - `grep -c 'grid-cols-3' src/screens/GameScreen.tsx` returns 1 and that occurrence is accompanied by `max-sm:grid-cols-1` on the same element
    - `grep -c "width: '160px'" src/components/piles/DiscardPile.tsx` returns 0
    - `src/components/piles/DiscardPile.tsx` contains `w-40`, `max-sm:w-32`, and still contains both `discard-pile-area` and `discard-pile-cards`
    - `src/components/piles/DrawPile.tsx` still contains `draw-pile-card`
    - `grep -rc 'portrait:\|landscape:' src/screens/GameScreen.tsx src/components/piles/` returns 0 for every file (D-12)
    - `git diff --exit-code tailwind.config.js` succeeds (D-10) and `git diff --exit-code src/components/Table.tsx` succeeds (no cross-plan edit)
    - `npm test -- --run` (full suite) exits 0, including the existing `.draw-card-ghost` animation tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Below 640px the board stacks table over piles, wraps the pile cluster, gives each player tile its own row and each action button full width, with the desktop arrangement byte-identical above the breakpoint and the draw-animation selectors intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grow every remaining undersized game-screen control to 44px</name>
  <files>src/screens/GameScreen.tsx, src/__tests__/screens/GameScreen.test.tsx</files>
  <read_first>
    - `src/screens/GameScreen.tsx` — as modified by Task 1; read the whole file so the audit catches controls the pattern map did not list
    - `.planning/phases/03-responsive-ui/03-PATTERNS.md` — the `src/screens/GameScreen.tsx` "Undersized controls" list: the Rules and Leave Game header icon buttons (`p-2`), the leave-game-confirm buttons (`px-4 py-2`), the pick-up-confirmation buttons (`px-4 py-2`), and the celebration modal's Dismiss `X` button (icon-only, no padding wrapper — the smallest control in the file)
    - `.planning/phases/03-responsive-ui/03-UI-SPEC.md` — the 44px spacing exception and the rule that the accent stays on primary CTAs
    - `.planning/phases/03-responsive-ui/03-05-SUMMARY.md` — the celebration modal is now a focus-trapped dialog whose Dismiss button is the first-focus target; resizing it must not disturb that
  </read_first>
  <behavior>
    - Test 1: the Rules header button and the Leave Game header button both carry `min-h-11` and `min-w-11`
    - Test 2: the celebration modal's `aria-label="Dismiss"` button carries `min-h-11` and `min-w-11`
    - Test 3: the celebration modal's Dismiss button is still the element that receives focus when the dialog opens — the resize did not change the focusable order
    - Test 4: the leave-game confirmation's "Keep Playing" and "Leave Game" buttons both carry `min-h-11`
    - Test 5: the pick-up confirmation's "Cancel" and "Pick Up Anyway" buttons both carry `min-h-11`
    - Test 6: the Play and Pick Up Pile buttons resolve to at least 44px (they already use `py-3`; assert whichever class provides it) and their `disabled` expressions are unchanged
    - Test 7: the Test Mode control-player `<select>` carries `min-h-11`
    - Test 8: the offline / auto-picking-up badges and the player-tile stat lines are unchanged — they are status text, not controls
  </behavior>
  <action>
    Audit every interactive element in `src/screens/GameScreen.tsx` by reading the file, then add `min-h-11` (and `min-w-11` for icon-only controls) to each one under 44px.

    Confirmed targets:
    - the Rules header button and the Leave Game header button (both `p-2` around a 24px icon, ~40px) — add `min-h-11 min-w-11 inline-flex items-center justify-center`, keeping their `title`, `aria-label="Leave Game"` and background colours
    - the celebration modal's Dismiss button in **both** variants (`absolute top-3 right-3` icon-only) — add `min-h-11 min-w-11 inline-flex items-center justify-center`, keeping `aria-label="Dismiss"`, `onClick={dismissCelebration}` and the absolute positioning. This button is the focus trap's first-focus target after plan 03-05, so it must remain the first focusable descendant of the dialog — do not wrap it in a new focusable element or move it in the DOM order
    - the leave-game-confirm "Keep Playing" and "Leave Game" buttons (`px-4 py-2`, ~40px) — add `min-h-11`
    - the pick-up-confirmation "Cancel" and "Pick Up Anyway" buttons (`px-4 py-2`, ~40px) — add `min-h-11`
    - the Test Mode control-player `<select>` — add `min-h-11` for consistency even though `px-4 py-3` likely already clears the bar; the explicit minimum makes the contract assertable

    The Play and Pick Up Pile buttons already use `py-3` on `font-bold` text, which clears 44px. Add `min-h-11` to both anyway so a single grep proves the whole screen's contract rather than requiring per-control arithmetic.

    Out of scope, deliberately: the offline / auto-picking-up badges, the player-tile stat lines, the `lastAction` readout and the Test Mode console output. All are non-interactive text; RESP-03 covers controls.

    Do not change any label, `aria-label`, `title`, `disabled` expression, handler or colour. Do not convert the pick-up or leave-game confirmations into `role="dialog"` — D-07 scopes RESP-05's dialog work to the celebration modal only, and Phase 6's shared `Modal` primitive owns the rest.

    Extend `src/__tests__/screens/GameScreen.test.tsx` with the eight behaviours above, reusing the existing `renderGame` harness.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/screens/GameScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'min-h-11' src/screens/GameScreen.tsx` returns at least 11
    - `grep -c 'min-w-11' src/screens/GameScreen.tsx` returns at least 4 (Rules, Leave Game, two Dismiss buttons)
    - `grep -c 'aria-label="Dismiss"' src/screens/GameScreen.tsx` still returns 2 and each of those buttons carries `min-h-11 min-w-11`
    - `grep -c 'role="dialog"' src/screens/GameScreen.tsx` still returns 1 — the pick-up and leave-game confirmations were not converted (D-07)
    - `src/screens/GameScreen.tsx` still contains `aria-label="Leave Game"` and both `disabled={(!revealedFaceDown && selectedCards.length === 0) || !isMyTurn}` and `disabled={gameState.discardPile.length === 0 || !isMyTurn}` unchanged
    - The offline badge spans still carry `text-xs px-2 py-1` and no `min-h-11`
    - All eight behaviours are asserted by named `it(...)` blocks, including test 3's focus assertion proving plan 03-05's trap still lands on the Dismiss button
    - `npm test -- --run` (full suite) exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Every interactive control on the game screen resolves to at least 44px in both dimensions, the celebration dialog's focus contract is intact, and no label, gate or handler changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control sizing → destructive actions | Leave Game, Pick Up Anyway and Play are all resized; a larger target on a destructive action must not become an easier accidental trigger without its existing confirmation |
| layout change → focus order | Reflowing containers can reorder the DOM, which would change tab order and the focus trap's first-focus target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | celebration dialog focus containment after resize | mitigate | The Dismiss button is resized in place with no wrapper element and no DOM reordering, so it remains the dialog's first focusable descendant. Task 2 test 3 asserts the trap still focuses it on open, catching any regression of plan 03-05's contract |
| T-03-13 | Tampering | destructive-control sizing | mitigate | Leave Game and Pick Up Pile both keep their existing confirmation portals; only class text changes and every `disabled` expression is asserted unchanged. A larger tap target is reached through the same two-step confirmation it always was |
| T-03-14 | Tampering | draw-animation DOM selectors | mitigate | `GameScreen.tsx`'s draw animation queries `.hand-area`, `.discard-pile-area`, `.discard-pile-cards` and `.draw-pile-card` by class name. Task 1 preserves all four (asserted), and the full suite including the `.draw-card-ghost` tests must pass — a silent selector break would otherwise strand the animation with no type error |
| T-03-04 | Elevation of Privilege | resized action buttons | mitigate | No handler or gate is touched; `playCards`/`pickUpPile` still route through `GameContext.dispatchMove` → `applyMove`, and the server-side `apply-move` Edge Function still validates every move regardless of what the client renders |
| T-03-SC | Tampering | npm installs | accept | No packages installed; 03-RESEARCH.md records zero new dependencies for the phase |
</threat_model>

<verification>
- `npm test -- --run` — full suite green, including the timed `.draw-card-ghost` assertions that would catch a broken animation selector
- `npm run build`
- `git diff --exit-code tailwind.config.js src/components/Table.tsx` — no breakpoint overrides and no cross-plan file edits
- `npx eslint src/screens/GameScreen.tsx src/components/piles/ src/__tests__/screens/GameScreen.test.tsx` — no new violations beyond the pre-existing `GameScreen.tsx:107` issue
</verification>

<success_criteria>
- The board stacks, the piles wrap, the player tiles go single-column and the action buttons go full-width below 640px, with the desktop arrangement unchanged above it
- The discard pile's fixed 160px inline width is gone, replaced by responsive classes
- Every interactive game-screen control carries `min-h-11`, and icon-only ones also carry `min-w-11`
- The celebration dialog's focus trap still lands on the Dismiss button
- No custom breakpoint, orientation variant, or global scale transform was introduced
</success_criteria>

<output>
Create `.planning/phases/03-responsive-ui/03-07-SUMMARY.md` when done
</output>
