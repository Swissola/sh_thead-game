---
phase: 03-responsive-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/components/Card.tsx
  - src/__tests__/components/Card.test.tsx
autonomous: true
requirements: [RESP-02, RESP-04]

must_haves:
  truths:
    - "A card can receive keyboard focus and be toggled in or out of selection with Enter or Space (D-02)"
    - "A keyboard-focused card shows a cyan focus ring that stays legible alongside the yellow selected ring and the hover lift (D-04)"
    - "A screen-reader user focusing an unplayable face-up card hears the same explanation a sighted mouse user sees on hover (Pitfall 4)"
    - "A face-down card's rank and suit never appear in any attribute or text before the play is committed (D-08, blind-play invariant)"
  artifacts:
    - path: "src/types.ts"
      provides: "CardProps extended with the option-role, selection-state, label, tabIndex and focus props"
      contains: "ariaSelected"
    - path: "src/components/Card.tsx"
      provides: "Keyboard-activatable, ARIA-labelled card leaf with a focus-visible ring"
      contains: "focus-visible:ring-cyan-400"
    - path: "src/__tests__/components/Card.test.tsx"
      provides: "Wave 0 test scaffold for RESP-02/RESP-04 per-card ARIA assertions"
  key_links:
    - from: "src/components/Card.tsx onKeyDown"
      to: "the same onClick callback the mouse path already uses"
      via: "Enter/Space invoking onClick, not a parallel handler"
      pattern: "onClick\\?\\.\\(\\)"
    - from: "src/components/Card.tsx face-down branch"
      to: "ariaLabel prop"
      via: "fixed non-identifying string supplied by the parent"
      pattern: "aria-label"
---

<objective>
Make the card leaf keyboard-operable and screen-reader-legible: `role="option"`, `aria-selected`, an externally-controlled `tabIndex`, Enter/Space activation routed through the existing click callback, a distinct `focus-visible` ring, and an accessible name — with the blind-play invariant preserved for face-down cards.

Purpose: `Hand.tsx` (03-03) and `Table.tsx` (03-04) become listbox containers in Wave 2. They need a card that already speaks `role="option"` and already handles Enter/Space, otherwise both plans would each reimplement the same leaf behaviour.
Output: an extended `CardProps` contract, an accessible `Card.tsx`, and the first dedicated `Card.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-responsive-ui/03-CONTEXT.md
@.planning/phases/03-responsive-ui/03-RESEARCH.md
@.planning/phases/03-responsive-ui/03-PATTERNS.md
@.planning/phases/03-responsive-ui/03-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Option semantics and Enter/Space activation on Card</name>
  <files>src/types.ts, src/components/Card.tsx, src/__tests__/components/Card.test.tsx</files>
  <read_first>
    - `src/components/Card.tsx` — the whole file; note the two separate returns (face-down branch starting at the `if (faceDown)` guard, face-up branch after it) and that the face-down outer div wires `onClick={onClick}` unconditionally while the face-up outer div wires `onClick={selectable ? onClick : undefined}`
    - `src/types.ts` — the existing `CardProps` interface (`card`, `faceDown`, `onClick`, `selectable`, `selected`, `small`, `title`)
    - `src/components/Hand.tsx` — how `title` is currently computed and passed ("Not your turn", "Play from face-up or face-down first", `First turn: only {rank}s allowed`, plus `getCardPlayability`'s tooltip)
    - `src/components/Table.tsx` — the face-down `CardComponent` call site, which passes no `title` today
    - `src/__tests__/testUtils/buildGameState.ts` — `buildCard` for fixtures
    - `src/__tests__/components/Toast.test.tsx` — the repo's component-test style
    - `.planning/phases/03-responsive-ui/03-UI-SPEC.md` — the Copywriting Contract rows for face-down and face-up accessible names
    - `CLAUDE.md` — the blind-play testing gotcha
  </read_first>
  <behavior>
    - Test 1: a face-up Card given `role="option"` and `tabIndex={0}` renders an outer element with `role="option"` and `tabindex="0"`
    - Test 2: a face-up Card with `selectable` and `role="option"`, focused, then `fireEvent.keyDown(el, { key: 'Enter' })`, calls `onClick` exactly once
    - Test 3: the same with `{ key: ' ' }` calls `onClick` exactly once
    - Test 4: a face-up Card with `selectable={false}` does not call `onClick` on either Enter or Space (mirrors its `onClick={selectable ? onClick : undefined}` gating)
    - Test 5: a face-down Card with `role="option"` calls `onClick` on Enter and on Space (mirrors its unconditional `onClick={onClick}` gating)
    - Test 6: a Card rendered without a `role` prop has no `aria-selected` attribute at all
    - Test 7: `role="option"` with `ariaSelected={true}` renders `aria-selected="true"`; with `ariaSelected={false}` renders `aria-selected="false"`
    - Test 8: `{ key: 'ArrowRight' }` on a Card does not call `onClick` and does not call `preventDefault` — arrows must keep bubbling to the listbox container
    - Test 9 (blind-play invariant): a face-down Card built from a known rank/suit fixture and given `ariaLabel="Face-down card"` has an accessible name of exactly "Face-down card", and the rendered element's `outerHTML` contains neither that card's `rank` string nor its `suit` character
  </behavior>
  <action>
    Extend `CardProps` in `src/types.ts` with these five optional members, adding `import type { FocusEventHandler } from 'react';` at the top of the file if no React type import exists yet:
    - `role?: 'option'`
    - `ariaSelected?: boolean`
    - `ariaLabel?: string`
    - `tabIndex?: number`
    - `onFocus?: FocusEventHandler<HTMLDivElement>`

    Do NOT add an `onKeyDown` prop. Card owns Enter/Space itself; arrow keys are left to bubble to the listbox container that plan 03-03/03-04 will add.

    In `src/components/Card.tsx`, destructure the five new props from the component's props object and apply them to the outer `div` of **both** branches:
    - `role={role}`
    - `aria-selected={role === 'option' ? ariaSelected : undefined}` — `aria-selected` is only valid on an `option` role, so it must be omitted entirely when `role` is undefined
    - `aria-label={ariaLabel}`
    - `tabIndex={tabIndex}`
    - `onFocus={onFocus}`

    Add an inline `onKeyDown` handler to each branch's outer `div` that checks `event.key === ' ' || event.key === 'Enter'`, calls `event.preventDefault()` (stops Space scrolling the page), and then invokes the same callback that branch's `onClick` invokes — the face-down branch calls `onClick?.()` unconditionally, the face-up branch calls `onClick?.()` only when `selectable` is truthy. Do not duplicate any selection logic here; the whole point is that keyboard and mouse share one callback (03-PATTERNS.md "Shared Patterns").

    Leave the existing `onMouseEnter`/`onMouseLeave` tooltip timer blocks, the `title` tooltip div and its `aria-hidden="true"`, the `deckColorMap`, and all existing className expressions structurally untouched in this task.

    Create `src/__tests__/components/Card.test.tsx` covering the nine behaviours above. Use `buildCard` from `src/__tests__/testUtils/buildGameState.ts` for fixtures and `fireEvent` from `@testing-library/react` — `@testing-library/user-event` is not installed. Test 9 is the blind-play regression guard: assert against `outerHTML` for the literal rank and suit strings, not just the accessible name.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/components/Card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/types.ts` `CardProps` declares all five of `role`, `ariaSelected`, `ariaLabel`, `tabIndex`, `onFocus`, and declares no `onKeyDown`
    - `grep -c 'onKeyDown' src/components/Card.tsx` returns 2 — one handler per branch, none passed in as a prop
    - `grep -c "role === 'option' ? ariaSelected : undefined" src/components/Card.tsx` returns 2
    - All nine behaviours are asserted by named `it(...)` blocks in `src/__tests__/components/Card.test.tsx`
    - `npm test -- --run src/__tests__/components/Card.test.tsx` exits 0
    - `npm test -- --run src/__tests__/screens/GameScreen.test.tsx` exits 0 — the existing harness still passes, proving no mouse-path regression
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Card accepts option semantics from its parent, activates on Enter and Space through its existing click callback, lets arrows bubble, and provably never leaks a face-down card's identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Distinct keyboard focus ring and focus-revealed playability text</name>
  <files>src/components/Card.tsx, src/__tests__/components/Card.test.tsx</files>
  <read_first>
    - `src/components/Card.tsx` — as modified by Task 1; specifically the two className template literals containing `${selectable ? 'hover:scale-110 hover:-translate-y-2 shadow-lg' : ''}` and `${selected ? 'scale-110 -translate-y-3 ring-4 ring-yellow-400' : ''}`
    - `.planning/phases/03-responsive-ui/03-UI-SPEC.md` — the Color section's exact focus-ring utility string and its "never applied to the accent" rule
    - `.planning/phases/03-responsive-ui/03-RESEARCH.md` — "Code Examples > Distinct Focus Ring" and Pitfall 4
    - `CLAUDE.md` — the ephemeral-timed-UI testing gotcha (assert immediately after `fireEvent`, never inside `waitFor`)
  </read_first>
  <behavior>
    - Test 1: both branches' rendered `class` attribute contains `focus-visible:ring-cyan-400`
    - Test 2: a `selected` Card still carries `ring-yellow-400` in its class list alongside the focus-ring utilities — the two compose, neither replaces the other
    - Test 3: `fireEvent.focus` on a face-up Card that has a `title` prop makes the tooltip element carry `opacity-100`, asserted immediately after the event (not inside `waitFor`)
    - Test 4: `fireEvent.blur` on the same Card returns the tooltip to `opacity-0`, asserted immediately
    - Test 5: a face-up Card with `title="Not your turn"` and `ariaLabel="Not your turn"` exposes "Not your turn" as its accessible name, so an AT user gets what the hover tooltip gives a mouse user
    - Test 6: a Card given both an `onFocus` prop and a `title` fires the caller's `onFocus` exactly once on focus (the roving-tabindex hook's cursor sync must not be swallowed)
  </behavior>
  <action>
    Append the focus-ring utilities to the className template literal of **both** branches in `src/components/Card.tsx`, as a plain (non-interpolated) segment placed after the `${selected ? ... }` interpolation so it composes rather than competes: `focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-cyan-400 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-900`. Use `focus-visible:`, never bare `focus:` — the ring must not flash on a mouse click that incidentally focuses the element (D-04, 03-UI-SPEC.md Color).

    Do not touch the `ring-4 ring-yellow-400` selected expression or the `hover:scale-110 hover:-translate-y-2` hover expression. All three states must stay independently legible.

    Close Pitfall 4's sighted-keyboard-user gap: in both branches, replace the plain `onFocus={onFocus}` wiring from Task 1 with an inline handler that calls `setHover(true)` and then `onFocus?.(event)`, and add a matching `onBlur` handler that calls `setHover(false)`. These fire immediately — do **not** route them through the 250ms/100ms `showTimer`/`hideTimer` pair the mouse path uses. Hover delay exists to stop tooltips flickering as a pointer sweeps across a fanned hand; keyboard focus is deliberate and should reveal without delay. Clear any pending `showTimer`/`hideTimer` in both handlers so a stale mouse timer cannot overwrite the focus-driven state.

    Do not add any touch-triggered reveal mechanism — D-08 explicitly rules that out for RESP-02; the requirement is satisfied by the card face's own printed rank/suit staying legible plus the accessible name added in Task 1.

    Typography (03-UI-SPEC.md's Label row): in both branches' tooltip `div`, change `text-sm font-medium` to `text-sm font-semibold`. This is the tooltip/aria-label text UI-SPEC.md's Label row governs (14px / weight 600), grown from the pre-existing 500 (`font-medium`) for consistency with the other Label-row text this phase touches (sort buttons in 03-03, turn announcer in 03-05).

    Extend `src/__tests__/components/Card.test.tsx` with the six behaviours above.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/components/Card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'focus-visible:ring-cyan-400' src/components/Card.tsx` returns 2
    - `grep -c 'ring-4 ring-yellow-400' src/components/Card.tsx` returns 2 — the selected ring is untouched
    - `grep -c 'focus:ring' src/components/Card.tsx` returns 0 when the `focus-visible:` matches are excluded (no bare `focus:` variant anywhere)
    - `grep -c 'onTouchStart\|onTouchEnd\|onPointerDown' src/components/Card.tsx` returns 0 — no touch reveal mechanism was added (D-08)
    - `grep -c 'onBlur' src/components/Card.tsx` returns 2
    - `grep -c 'text-sm font-semibold' src/components/Card.tsx` returns 2 — both tooltip divs carry the UI-SPEC Label weight
    - `grep -c 'font-medium' src/components/Card.tsx` returns 0 — no stale 500-weight tooltip text remains
    - All six behaviours are asserted by named `it(...)` blocks; the tooltip opacity assertions appear immediately after their `fireEvent` call with no enclosing `waitFor`
    - `npm test -- --run src/__tests__/components` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Focused, selected and hovered cards are three visually distinct states; a keyboard or screen-reader user gets the same playability explanation a mouse user gets on hover; no touch reveal mechanism was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card state → accessibility tree | `aria-label` and `aria-selected` expose card information to assistive technology even when it is invisible on screen — the one place this phase could regress a game-integrity rule |
| keyboard input → selection state | Enter/Space become a second activation path alongside click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `Card.tsx` face-down branch | mitigate | The face-down branch renders whatever `ariaLabel` its parent supplies and never derives text from `card.rank`/`card.suit`; Task 1 test 9 asserts the rendered `outerHTML` contains neither the fixture's rank nor its suit. This is the blind-play invariant recorded in `CLAUDE.md`, and it is the only game-integrity rule this phase can break |
| T-03-04 | Elevation of Privilege | `Card.tsx` keyboard handler | mitigate | Enter/Space invoke the exact `onClick` callback the mouse path uses, gated by the same `selectable` check per branch. No new dispatch path is introduced; `GameContext.dispatchMove` → `applyMove` → the server-side `apply-move` Edge Function remain the only route to state change |
| T-03-07 | Spoofing | `Card.tsx` accessible name | accept | The face-up `aria-label` reuses the existing `title` string, which is composed in `uiLogic.ts`/`Hand.tsx` from fixed literals and card ranks — never from user-supplied text. No new untrusted string reaches an attribute here |
| T-03-SC | Tampering | npm installs | accept | No packages installed in this plan; 03-RESEARCH.md records zero new dependencies for the phase |
</threat_model>

<verification>
- `npm test -- --run src/__tests__/components` — new `Card.test.tsx` green alongside the existing `Toast.test.tsx`
- `npm test -- --run src/__tests__/screens/GameScreen.test.tsx` — existing mouse-path integration coverage unaffected
- `npm run build` — strict TypeScript clean with the widened `CardProps`
- `npx eslint src/types.ts src/components/Card.tsx src/__tests__/components/Card.test.tsx`
</verification>

<success_criteria>
- `Card` accepts `role`, `ariaSelected`, `ariaLabel`, `tabIndex` and `onFocus` and applies them to both the face-down and face-up outer elements
- Enter and Space activate a card through its existing click callback; arrows are left to bubble
- Focused, selected and hovered are three visually distinct states, with the focus ring using `focus-visible:` and `ring-cyan-400`
- A face-down card provably exposes no rank or suit to the DOM or the accessibility tree
</success_criteria>

<output>
Create `.planning/phases/03-responsive-ui/03-02-SUMMARY.md` when done
</output>
