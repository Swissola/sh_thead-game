---
phase: 01-rules-engine-refactor
plan: 5
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/App.tsx
  - src/screens/GameScreen.tsx
  - src/screens/MenuScreen.tsx
  - src/screens/LobbyScreen.tsx
  - src/__tests__/screens/MenuScreen.test.tsx
  - src/__tests__/screens/LobbyScreen.test.tsx
autonomous: true
requirements: [ENGINE-03, ENGINE-05, ENGINE-07]

must_haves:
  truths:
    - "App.tsx no longer contains any screen JSX itself — it only generates playerId, wraps GameProvider, and routes to MenuScreen/LobbyScreen/GameScreen based on GameState.phase (D-09)"
    - "Room creation/joining failures show a toast instead of window.alert() (ENGINE-05)"
    - "Clicking 'Test Mode (3 Players)' or 'Test Mode (First Turn Ready)' on MenuScreen navigates to the game screen by setting gameState, not by setting a separate screen variable"
    - "LobbyScreen's Start Game button is disabled for non-hosts and for fewer than 2 players, matching today's behavior exactly"
  artifacts:
    - path: "src/App.tsx"
      provides: "slim orchestrator: playerId generation, GameProvider wrap, phase-based routing, shared background chrome, <Toast/>"
      min_lines: 5
    - path: "src/screens/MenuScreen.tsx"
      provides: "room creation/joining/test-mode entry, extracted from App.tsx:929-997"
    - path: "src/screens/LobbyScreen.tsx"
      provides: "room code display, player list, host-only start button, extracted from App.tsx:999-1063"
    - path: "src/screens/GameScreen.tsx"
      provides: "placeholder only in this plan — reads gameState.phase and renders a minimal stand-in; Plan 01-06 replaces the full contents"
  key_links:
    - from: "src/App.tsx"
      to: "src/context/GameContext.tsx"
      via: "GameProvider playerId={playerId} wrapping the router"
      pattern: "GameProvider"
    - from: "src/screens/MenuScreen.tsx"
      to: "src/context/GameContext.tsx"
      via: "useGameContext() for setGameState/setTestMode/playerId/setControllingPlayer/showToast"
      pattern: "useGameContext\\(\\)"
    - from: "src/screens/LobbyScreen.tsx"
      to: "src/gameLogic.ts"
      via: "createDeck/shuffleDeck calls in startGame"
      pattern: "GameLogic\\.(createDeck|shuffleDeck)"
---

<objective>
Turn `App.tsx` into the slim D-09 orchestrator (playerId generation, `GameProvider` wiring, phase-based routing, shared background chrome, toast container) and extract `MenuScreen`/`LobbyScreen` from its current menu/lobby JSX branches, converting their `alert()` calls to toasts. A minimal placeholder `GameScreen.tsx` is created so the orchestrator's routing compiles and runs standalone — Plan 01-06 replaces its contents with the full game screen.

Purpose: This is ENGINE-03's structural split made real for the two simpler screens, and ENGINE-05's alert-to-toast conversion for the six menu/lobby-level alerts. Routing driven by `GameState.phase` (rather than a separate `screen` state variable) removes a redundant piece of state that today does double duty and can drift from the actual game phase.
Output: `App.tsx` (orchestrator), `MenuScreen.tsx`, `LobbyScreen.tsx`, a placeholder `GameScreen.tsx`, RTL smoke tests for the two new screens per D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rules-engine-refactor/01-CONTEXT.md
@.planning/phases/01-rules-engine-refactor/01-RESEARCH.md
@.planning/phases/01-rules-engine-refactor/01-PATTERNS.md
@src/context/GameContext.tsx
@src/components/Toast.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx orchestrator — playerId, GameProvider, phase-based routing (D-09, D-10)</name>
  <files>src/App.tsx, src/screens/GameScreen.tsx</files>
  <read_first>
    src/App.tsx (whole file — this task deletes almost everything and replaces it; note the current `screen` state at line 57, the never-set `playerId` at lines 60-62, the poll effect at lines 331-353, the three `min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900` wrapper divs at lines 931, 1003, 1076)
    src/context/GameContext.tsx (GameProvider's prop surface — only `playerId`/`children` — and useGameContext's exposed fields, from Plan 01-04)
    src/components/Toast.tsx (Toast's props)
  </read_first>
  <action>
    Rewrite `src/App.tsx` to export a default `ShitheadGame` component that: generates `const [playerId] = useState(() => crypto.randomUUID())` (fixing the never-set `playerId` bug named in CONTEXT.md's code_context — remove the `@ts-expect-error`/`eslint-disable-next-line` comments since `playerId` is now genuinely read); renders `<GameProvider playerId={playerId}><Router /></GameProvider>` where `Router` is a component defined in the same file (or a second export) that calls `useGameContext()` for `gameState`, `testMode`, `toast`, `dismissToast`, `setGameState`.
    `Router` renders one shared outer `<div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 p-4">` (D-09's "shared chrome," hoisted from the three near-duplicate wrapper divs — note the current menu/lobby wrappers additionally have `flex items-center justify-center` which the game wrapper does not; MenuScreen and LobbyScreen must each add their own inner `flex items-center justify-center` wrapper around their content since only those two need centering, GameScreen does not) containing: `{!gameState && <MenuScreen />}`, `{gameState?.phase === 'lobby' && <LobbyScreen />}`, `{gameState && gameState.phase !== 'lobby' && <GameScreen />}`, and `<Toast toast={toast} onDismiss={dismissToast} />` once, after the conditional screens.
    Port the poll effect (`App.tsx:331-353`) into `Router`: `useEffect(() => { if (!gameState || testMode) return; const interval = setInterval(async () => { const result = await window.storage.get(\`game:${gameState.roomCode}\`, true); if (result) { setGameState(JSON.parse(result.value)); } }, 2000); return () => clearInterval(interval); }, [gameState?.roomCode, testMode, setGameState])`. This reuses the context's `setGameState` for the poll's write-back; accept the minor redundant re-persist this causes on every poll tick — it reproduces today's exact behavior and this whole polling mechanism is replaced outright by Supabase Realtime in Phase 2 (MPLAY-02), so it is not worth a bespoke read-only setter for one phase's lifetime.
    Do not port the `screen` state, the console-log interception effect, `showRules`, `drawingCards`, `pickUpConfirmation`, `celebrationModal`, or `consoleLogs` — those move into `GameScreen.tsx` in Plan 01-06 (they are game-screen-local UI/animation state per RESEARCH.md's anti-pattern guidance, not orchestrator concerns).
    Create `src/screens/GameScreen.tsx` as a minimal placeholder: `export function GameScreen() { const { gameState } = useGameContext(); return <div className="text-white p-8">Game screen placeholder — phase: {gameState?.phase}</div>; }`. Plan 01-06 replaces this file's entire contents with the full extraction; this placeholder exists solely so `Router`'s routing and this plan's own tests are self-contained and pass independently of Plan 01-06.
  </action>
  <acceptance_criteria>
    - `src/App.tsx` contains no JSX for card rendering, hand/table composition, or celebration/pickup modals (grep confirms no `Table`/`Hand`/`celebrationModal`/`pickUpConfirmation` identifiers remain in `App.tsx`)
    - `src/App.tsx` contains `crypto.randomUUID()` and no `@ts-expect-error` comment
    - `npx tsc --noEmit` exits 0
    - `npm run dev` starts without a runtime error (manual smoke check acceptable here since this is routing/wiring, not business logic — automated coverage for Menu/Lobby's actual behavior is Task 2/3's job)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>App.tsx is a slim orchestrator with no screen-specific JSX; routing is driven entirely by GameState.phase; a placeholder GameScreen satisfies the 'game' route until Plan 01-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MenuScreen extraction (ENGINE-03, ENGINE-05, D-12)</name>
  <files>src/screens/MenuScreen.tsx, src/__tests__/screens/MenuScreen.test.tsx</files>
  <read_first>
    src/App.tsx lines 151-291 (createTestGame, createTestGameStarted, createRoom, joinRoom — the four functions to port) and lines 929-997 (the menu JSX to extract)
    src/context/GameContext.tsx (setGameState/setTestMode/playerId/setControllingPlayer/showToast — the exact context fields MenuScreen consumes)
    src/gameLogic.ts (createDeck, shuffleDeck, getStartingPlayer — from Plan 01-01)
  </read_first>
  <behavior>
    - Clicking "Test Mode (3 Players)" with any `playerName` value calls `setTestMode(true)` and `setGameState` with a 3-player `phase:'setup'` state (no dispatch assertion needed — assert on the resulting DOM/context state per RTL convention, or spy on the context functions via a test wrapper).
    - Clicking "Create Room" with an empty `playerName` shows a toast (via `showToast`) reading "Please enter your name" and does not call `setGameState`.
    - Clicking "Join" with a non-empty name and room code, when the room doesn't exist (`window.storage.get` resolves undefined), shows a toast reading "Room not found".
  </behavior>
  <action>
    Create `src/screens/MenuScreen.tsx` exporting `MenuScreen()`. Local state: `playerName` (string, `''`), `roomCodeInput` (string, `''`) — do not name this `roomCode`, since the room's actual code lives on `gameState.roomCode` once one exists; this local field is only the join-room text input. Read `setGameState`, `setTestMode`, `playerId`, `setControllingPlayer`, `showToast` from `useGameContext()`.
    Port and rewire, replacing every `alert(...)` call with `showToast(...)` (same message text, no code needed):
    - `createTestGame` (from `App.tsx:151-188`): builds 3 unready test players via `GameLogic.createDeck`/`shuffleDeck`, calls `setTestMode(true)` then `setGameState(newGameState)`. Do not call any celebration-reset function — celebration state now lives in `GameScreen` and resets naturally on mount (D-10 cleanup, per Plan 01-05 Task 1's routing).
    - `createTestGameStarted` (from `App.tsx:190-231`): same dealing logic with `isReady:true`, additionally computes `startingPlayerIndex = GameLogic.getStartingPlayer(dealtPlayers)`, sets `phase:'playing'`, `currentTurn: startingPlayerIndex`, and calls `setControllingPlayer(startingPlayerIndex)` from context (matching `App.tsx:229`'s test-mode player-switcher behavior).
    - `createRoom` (from `App.tsx:233-261`): validates `playerName.trim()` (toast "Please enter your name" if empty), generates `code = Math.random().toString(36).substr(2,6).toUpperCase()`, builds a `phase:'lobby'` GameState with `host: playerId`, wraps `await setGameState(newGameState)` in try/catch showing toast "Failed to create room" on failure. Do not call `window.storage.set` directly — `setGameState` (from context) already persists per Plan 01-04's design; calling it again here would duplicate the write.
    - `joinRoom` (from `App.tsx:263-291`): validates `playerName.trim() && roomCodeInput.trim()` (toast "Please enter your name and room code"), fetches via `await window.storage.get(\`game:${roomCodeInput.toUpperCase()}\`, true)` (toast "Room not found" if absent), parses, validates `phase === 'lobby'` (toast "Game has already started" if not), pushes a new player with `id: playerId`, then `await setGameState(state)` wrapped in try/catch showing toast "Failed to join room" on failure.
    Port the JSX from `App.tsx:929-997` into `MenuScreen`'s return, wrapped in its own `<div className="flex items-center justify-center">` inner wrapper (per Task 1's note — the orchestrator's outer div no longer centers content), referencing local `playerName`/`roomCodeInput` state and the four functions above.
    Write `src/__tests__/screens/MenuScreen.test.tsx` per the `<behavior>` spec, wrapping `MenuScreen` in `GameProvider` from `../../context/GameContext` (with a fixed `playerId` prop) for rendering, using `@testing-library/react`'s `render`/`screen`/`fireEvent`, and mocking `window.storage.get`/`set` (already a global per `src/storage.ts` — check its shape before mocking) for the join-room-not-found case.
  </action>
  <acceptance_criteria>
    - `MenuScreen.tsx` contains zero `alert(` calls (grep -c returns 0)
    - `npx vitest run src/__tests__/screens/MenuScreen.test.tsx` exits 0
    - Grep confirms `MenuScreen.tsx` does not call `window.storage.set` directly (only `joinRoom`'s `window.storage.get` read remains as a direct storage call)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screens/MenuScreen.test.tsx</automated>
  </verify>
  <done>MenuScreen renders and behaves identically to today's menu branch, with all alerts converted to toasts and room persistence routed through the single GameContext setGameState seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LobbyScreen extraction (ENGINE-03, D-12)</name>
  <files>src/screens/LobbyScreen.tsx, src/__tests__/screens/LobbyScreen.test.tsx</files>
  <read_first>
    src/App.tsx lines 293-323 (startGame) and lines 999-1063 (the lobby JSX to extract)
    src/context/GameContext.tsx (gameState/playerId/setGameState — the fields LobbyScreen consumes)
    src/gameLogic.ts (createDeck, shuffleDeck)
  </read_first>
  <behavior>
    - Renders the room code from `gameState.roomCode` (not a locally-tracked value) and the current player list with a crown icon next to `gameState.host`.
    - The "Start Game" button is present and enabled only when `gameState.host === playerId && gameState.players.length >= 2`; clicking it calls `setGameState` with a `phase:'setup'` state where every player has a freshly dealt 3/3/3 hand/faceUp/faceDown.
    - A non-host player sees "Waiting for host to start the game..." instead of a button.
  </behavior>
  <action>
    Create `src/screens/LobbyScreen.tsx` exporting `LobbyScreen()`. Read `gameState`, `playerId`, `setGameState` from `useGameContext()`. Local state: `copied` (boolean, `false`).
    `copyRoomCode`: `navigator.clipboard.writeText(gameState.roomCode); setCopied(true); setTimeout(() => setCopied(false), 2000)` (ported from `App.tsx:325-329`, using `gameState.roomCode` instead of the old local `roomCode` state).
    `startGame` (from `App.tsx:293-323`): guard `if (!gameState || gameState.host !== playerId || gameState.players.length < 2) return;`; compute `numDecks = Math.ceil(gameState.players.length / 4)`, `deck = GameLogic.shuffleDeck(GameLogic.createDeck(numDecks))`; deal 3/3/3 to every player via `.splice(0,3)` three times each (matching `App.tsx:299-305` exactly), reset `isReady:false` for all; build the updated `phase:'setup'` state preserving `gameState.host`/`gameState.currentTurn`/`gameState.discardPile`/`gameState.burnPile`; call `await setGameState(updatedState)`.
    Port the JSX from `App.tsx:999-1063` into `LobbyScreen`'s return, wrapped in its own `<div className="flex items-center justify-center">` inner wrapper, reading `gameState.roomCode` for display (not a separate local variable), `gameState.players`, `gameState.host`, `playerId`, `copied`/`copyRoomCode`/`startGame`.
    Write `src/__tests__/screens/LobbyScreen.test.tsx` per the `<behavior>` spec: render `LobbyScreen` inside `GameProvider`, seed `gameState` via the provider's `setGameState` in a `beforeEach`/test setup (or by rendering a small test harness that calls `setGameState` on mount before asserting), verify the Start Game button's `disabled` state for both host-with-2-players and non-host scenarios, and verify clicking it (when enabled) results in `gameState.phase === 'setup'` with every player holding 3 hand + 3 faceUp + 3 faceDown cards.
  </action>
  <acceptance_criteria>
    - `LobbyScreen.tsx` reads the room code exclusively from `gameState.roomCode` (grep confirms no local `roomCode` state declaration)
    - `npx vitest run src/__tests__/screens/LobbyScreen.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screens/LobbyScreen.test.tsx</automated>
  </verify>
  <done>LobbyScreen renders and behaves identically to today's lobby branch, sourcing the room code from GameState rather than a duplicated local variable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| MenuScreen joinRoom -> storage | `roomCodeInput` (user-typed) is used directly as a `window.storage` lookup key after `.toUpperCase()` — no move/rule enforcement involved, just a lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-09 | Information Disclosure | `joinRoom`'s room-code lookup | accept | Room codes are 6-character base36 (~2×10^9 combinations), single-browser `localStorage`-backed today with no cross-user network exposure yet — guessing/enumeration risk is deferred to Phase 2 where Supabase-backed rooms get real access control (MPLAY-04). Not a regression introduced by this refactor. |
| T-01-10 | Elevation of Privilege | `LobbyScreen`'s host-only Start Game gating | mitigate | `startGame` is guarded both by the disabled button state and an internal `gameState.host !== playerId` early return inside the function itself — a non-host cannot trigger `startGame`'s effect even if the disabled attribute were bypassed client-side (e.g. via devtools), since the guard re-checks host identity before doing anything. |
</threat_model>

<verification>
`npx tsc --noEmit` exits 0. `npx vitest run src/__tests__/screens/MenuScreen.test.tsx src/__tests__/screens/LobbyScreen.test.tsx` exits 0. Run `npm test -- --run` for the full-suite regression check.
</verification>

<success_criteria>
App.tsx is a slim, phase-driven orchestrator with no screen JSX of its own. MenuScreen and LobbyScreen are fully extracted, alert-free, and pass D-12-style interaction smoke tests. A placeholder GameScreen keeps the app runnable pending Plan 01-06.
</success_criteria>

<output>
Create `.planning/phases/01-rules-engine-refactor/01-05-SUMMARY.md` when done
</output>
