---
phase: 01-rules-engine-refactor
plan: 3
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/engine/applyMove.ts
  - src/__tests__/engine/applyMove.test.ts
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-07]

must_haves:
  truths:
    - "applyMove's PLAY_CARDS case reproduces the existing burn/draw/win sequencing (shouldBurnPile, getCardsToDrawCount, hasPlayerWon, getNextPlayer) exactly, sourced only from gameLogic.ts predicates"
    - "A rejected PLAY_CARDS move (wrong turn, no selection, invalid combination, first-turn mismatch, bad indices) returns the original state reference unchanged, per D-02"
    - "A successful PLAY_CARDS move never mutates the input player's hand/faceUp/faceDown arrays — every touched array is a fresh reference, including the blind-play-fails-and-picks-up-pile branch and the draw-fill branch"
    - "applyMove no longer contains a PLAY_CARDS stub — the switch's four cases are all fully implemented"
  artifacts:
    - path: "src/engine/applyMove.ts"
      provides: "complete applyMove covering all four move types"
      exports: ["applyMove"]
    - path: "src/__tests__/engine/applyMove.test.ts"
      provides: "exhaustive PLAY_CARDS test coverage: valid play, burn (10s and four-of-a-kind), blind face-down play (valid and invalid), mixed hand+faceUp play, first-turn validation, win condition, empty-deck edge case, plus every distinct rejection reason"
  key_links:
    - from: "src/engine/applyMove.ts"
      to: "src/gameLogic.ts"
      via: "GameLogic.canPlayMultipleCards / shouldBurnPile / getPlayResult / getCardsToDrawCount / hasPlayerWon / isGameOver / getNextPlayer / canPlayMixedSources / getStartingCard calls"
      pattern: "GameLogic\\.(canPlayMultipleCards|shouldBurnPile|getPlayResult|getCardsToDrawCount|hasPlayerWon|isGameOver|getNextPlayer|canPlayMixedSources|getStartingCard)"
---

<objective>
Complete `applyMove` by implementing the `PLAY_CARDS` case — the most complex of the four move types, covering ordinary plays, burning the pile, blind face-down plays (including the "invalid blind play picks up the pile" branch), mixed hand+face-up plays when the deck is empty, first-turn rank validation, and the win/game-over check — with the same D-11 exhaustive test rigor as Plan 01-02.

Purpose: RESEARCH.md's Pitfall 1 identifies this as the widest surface for the direct-mutation bug (`App.tsx:611-673`'s `updatedPlayer.hand[index] = null` pattern recurs at five separate line ranges within the current `playCards` handler). This plan is where ENGINE-02's "no mutation, at every site" guarantee is proven for the phase's largest, riskiest function.
Output: `applyMove.ts` with all four `Move` cases fully implemented; `applyMove.test.ts` extended with the full PLAY_CARDS matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-rules-engine-refactor/01-CONTEXT.md
@.planning/phases/01-rules-engine-refactor/01-RESEARCH.md
@.planning/phases/01-rules-engine-refactor/01-PATTERNS.md
@src/engine/moves.ts
@src/engine/errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write exhaustive failing tests for PLAY_CARDS</name>
  <files>src/__tests__/engine/applyMove.test.ts</files>
  <read_first>
    src/App.tsx lines 461-834 (the full playCards handler being ported — read completely, this is the source of truth for every branch)
    src/engine/moves.ts (PLAY_CARDS's `cards: CardSelection[]` and optional `reorderedHand` fields)
    src/gameLogic.ts (canPlayMultipleCards, shouldBurnPile, getPlayResult, getCardsToDrawCount, hasPlayerWon, isGameOver, getNextPlayer, canPlayMixedSources, getStartingCard, getAvailableCardSource)
    src/__tests__/testUtils/buildGameState.ts (fixture builders — PLAY_CARDS tests will need players with specific hand/faceUp/faceDown contents and specific discardPile/deck states, so build these via overrides rather than the bare defaults)
  </read_first>
  <behavior>
    Add `describe('applyMove - PLAY_CARDS')` blocks covering, each asserting the D-11/ENGINE-02 reference-inequality + `structuredClone` input-unchanged pair from Plan 01-02:
    - Valid single-card play from hand: removes the card (sets its slot to `null`), adds it to `discardPile`, advances `currentTurn` via `GameLogic.getNextPlayer`.
    - Valid multi-card play (same rank) from hand: same as above for all selected indices.
    - Burn via 10: `discardPile`'s cards move to `burnPile`, `discardPile` becomes `[]`, `currentTurn` stays on the same player (does not advance).
    - Burn via four-of-a-kind: same burn behavior, triggered by rank-matching rather than a 10.
    - Draw-to-3 after a play: when `deck.length > 0` and the player's hand drops below 3 non-null cards, drawn cards fill `null` slots first then extend the array, and `deck` shrinks by the drawn count.
    - Win condition: a play that empties hand+faceUp+faceDown sets that player's cards to all-empty; when it's the second-to-last player remaining, `phase` becomes `'finished'`.
    - Blind face-down play, valid: `cards:[{type:'faceDown', index:N}]` with no prior selection, resolves the card via `player.faceDown[N]`, validates it, plays it if valid.
    - Blind face-down play, invalid: player's revealed-blind card cannot be played — hand receives the played card plus the entire former `discardPile` (filling nulls then extending), `discardPile` becomes `[]`, turn advances, no error (D-02: a blind miss is a valid move outcome, not a rejection — mirror `App.tsx:643-693` exactly).
    - Mixed hand+faceUp play: only valid when `deck.length === 0` and `GameLogic.canPlayMixedSources` returns true for the combined selection; reject `INVALID_COMBINATION` when `deck.length > 0` or when playing from a non-hand `cardSource` per `GameLogic.getAvailableCardSource`.
    - First-turn validation: when `state.isFirstTurn`, reject `FIRST_TURN_INVALID` if any selected card's rank doesn't match `GameLogic.getStartingCard(player)`'s rank; accept when it matches.
    - `reorderedHand` field: when present, `applyMove` treats it as the player's effective `hand` for the entire PLAY_CARDS computation (index resolution, removal, draw-fill) instead of `state.players[playerIndex].hand`.
    - Rejections: `NOT_YOUR_TURN` (playerId doesn't match `state.currentTurn`'s player), `WRONG_PHASE` (`state.phase !== 'playing'`), `NO_SELECTION` (empty `cards` and no face-down index), `INVALID_PLAY` (non-blind cards fail `canPlayMultipleCards`), `INVALID_SELECTION` (an index in `cards` is out of bounds or resolves to a `null` slot), `UNKNOWN_PLAYER`.
    - Empty-deck edge case: a play that would normally draw cards, when `deck.length === 0`, draws zero cards and doesn't error.
  </behavior>
  <action>
    Extend `src/__tests__/engine/applyMove.test.ts` with the full `describe('applyMove - PLAY_CARDS')` block per `<behavior>`. Remove or update any test from Plan 01-02 that asserted the temporary `PLAY_CARDS not yet implemented` stub error, since this task supersedes it. Use `buildGameState`/`buildPlayer`/`buildCard` overrides to construct precise fixtures for each scenario (e.g., a discard pile ending in a `10` for the burn case, a hand with exactly the `state.isFirstTurn` starting card for the first-turn case).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/engine/applyMove.test.ts` runs and fails only on the new PLAY_CARDS assertions (RED) — the READY_UP/SWAP_CARDS/PICK_UP_PILE tests from Plan 01-02 still pass
    - At least 16 distinct `it(...)` blocks across the PLAY_CARDS describe block (one per bullet above, several bullets need 2+ cases)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/engine/applyMove.test.ts</automated>
  </verify>
  <done>Exhaustive failing PLAY_CARDS test suite exists, committed before implementation; prior move-type tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement applyMove's PLAY_CARDS case</name>
  <files>src/engine/applyMove.ts</files>
  <read_first>
    src/__tests__/engine/applyMove.test.ts (the tests just written — the implementation target)
    src/App.tsx lines 461-834 (playCards — port this logic, dropping only the DOM/flushSync/setTimeout animation wrapper at lines 763-825; the burn/draw/win computation at lines 696-746 is pure and must be preserved)
    src/01-PATTERNS.md "Direct-mutation bug — what NOT to copy" and "Burn/draw/win sequencing to preserve exactly" sections
  </read_first>
  <action>
    Replace the `applyPlayCardsStub` from Plan 01-02 with a real `applyPlayCards(state, move, playerIndex)`:
    - Reject `WRONG_PHASE` if `state.phase !== 'playing'`; reject `NOT_YOUR_TURN` if `move.playerId !== state.players[state.currentTurn].id`; reject `NO_SELECTION` if `move.cards.length === 0`.
    - Resolve `effectiveHand = move.reorderedHand ?? player.hand` and use it for every hand-index resolution and mutation in this function (never fall back to `player.hand` once `reorderedHand` is supplied).
    - Resolve `cardSource = GameLogic.getAvailableCardSource({...player, hand: effectiveHand})`.
    - Determine `isBlindPlay = move.cards[0].type === 'faceDown'` (no separate "revealed" concept — the caller supplies the face-down index directly in `move.cards`).
    - If mixed hand+faceUp selection present: reject `INVALID_COMBINATION` unless `state.deck.length === 0 && cardSource === 'hand' && GameLogic.canPlayMixedSources(...)` — mirror `App.tsx:485-515` exactly, sourcing hand cards from `effectiveHand`.
    - Resolve `cardsToPlay` from `move.cards` against `effectiveHand`/`player.faceUp`/`player.faceDown` — reject `INVALID_SELECTION` if any index is out of bounds or resolves to `null`.
    - First-turn check: if `state.isFirstTurn`, reject `FIRST_TURN_INVALID` unless every card in `cardsToPlay` matches `GameLogic.getStartingCard({...player, hand: effectiveHand})`'s rank.
    - Non-blind validation: reject `INVALID_PLAY` unless `GameLogic.canPlayMultipleCards(cardsToPlay, state.discardPile)` (skip this check when `isBlindPlay`, matching today's behavior).
    - Build `updatedPlayer` by copying `effectiveHand`/`faceUp`/`faceDown` fresh (`[...effectiveHand]`, `[...player.faceUp]`, `[...player.faceDown]`) *before* any index-assignment — this is the exact bug class Pitfall 1 names; every one of the five original mutation sites (removal, blind-fail refill, draw-fill) must operate on these fresh arrays, never on `player.hand`/`player.faceUp`/`player.faceDown` directly.
    - Blind-play-fails branch: when `isBlindPlay && !GameLogic.canPlayMultipleCards(cardsToPlay, state.discardPile)`, port `App.tsx:643-693` (fill hand from cards-played + former discard pile, clear `discardPile`, advance turn via `GameLogic.getNextPlayer`) onto the fresh copied hand array — return success (not an error; a failed blind play is a valid move outcome per D-02).
    - Otherwise (valid play, blind or not): port `App.tsx:696-746`'s burn/draw/win sequencing verbatim, calling only `GameLogic.shouldBurnPile`, `GameLogic.getPlayResult`, `GameLogic.getCardsToDrawCount`, `GameLogic.hasPlayerWon`, `GameLogic.isGameOver`, `GameLogic.getNextPlayer` — remove selected cards from the fresh copied arrays (set to `null`), append to `newDiscardPile`, burn into `newBurnPile` if `shouldBurnPile`, compute `drawnCards` from `state.deck.slice(0, cardsToDraw)` and fill them into the fresh hand's `null` slots then extend, set `phase: gameOver ? 'finished' : 'playing'`, `isFirstTurn: false`, `currentTurn: burned ? playerIndex : GameLogic.getNextPlayer(...)`.
    - Wire the real `applyPlayCards` into the switch's `'PLAY_CARDS'` case, removing the `applyPlayCardsStub` function and its `TODO(01-03)` comment entirely.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/engine/applyMove.test.ts` exits 0 — all four move types' tests pass, including the full PLAY_CARDS matrix from Task 1
    - `npx tsc --noEmit` exits 0
    - Grep confirms zero remaining occurrences of `applyPlayCardsStub` or the `TODO(01-03)` marker in `src/engine/applyMove.ts`
    - Manual source check: every index-assignment (`arr[i] = ...`) in `applyPlayCards` operates on a `const`-declared fresh array from this function's own scope, never on `player.hand`/`player.faceUp`/`player.faceDown` or `effectiveHand` itself
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/engine/applyMove.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All four Move types are fully implemented in applyMove.ts, exhaustively tested per D-11, with zero input-state mutation at any of the five original bug sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caller -> applyMove (PLAY_CARDS) | `move.cards` indices and `move.reorderedHand` are untrusted input; PLAY_CARDS is the highest-value target since it's the only move type that changes hand contents, discard pile, and turn order in one call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-04 | Spoofing | `applyPlayCards` | mitigate | D-03 turn-ownership check identical to T-01-01, applied to PLAY_CARDS; tested explicitly (NOT_YOUR_TURN case). |
| T-01-05 | Tampering | `applyPlayCards` | mitigate | Every `CardSelection` index in `move.cards` is bounds-checked against the resolved source array's length and checked for a non-null card before use (INVALID_SELECTION), same V5 pattern as T-01-02, tested explicitly. |
| T-01-06 | Tampering | `applyPlayCards`'s `reorderedHand` field | accept | A caller could supply a `reorderedHand` that doesn't match the real hand's card multiset (e.g. duplicating a card). Phase 1 has no auth boundary to validate against a trusted source, and this field is only ever populated by the UI itself (GameScreen, Plan 01-06) from the player's own current hand — not user-facing input. Accepted for Phase 1; Phase 2's server-side validation (MPLAY-04) is the natural point to add a multiset-equality check against the authoritative server-side hand if `reorderedHand` survives into that design. |
</threat_model>

<verification>
`npx vitest run src/__tests__/engine/applyMove.test.ts` exits 0 covering all four move types. `npx tsc --noEmit` exits 0. Run `npm test -- --run` for the full-suite regression check.
</verification>

<success_criteria>
`applyMove.ts` is complete: all four move types implemented, zero mutation of input state at any level, D-11's exhaustive per-move-type test matrix green, ready for Plan 01-04 to wire into a `GameProvider`.
</success_criteria>

<output>
Create `.planning/phases/01-rules-engine-refactor/01-03-SUMMARY.md` when done
</output>
