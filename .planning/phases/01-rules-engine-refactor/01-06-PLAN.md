---
phase: 01-rules-engine-refactor
plan: 6
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - src/screens/GameScreen.tsx
  - src/main.tsx
  - src/__tests__/screens/GameScreen.test.tsx
autonomous: true
requirements: [ENGINE-01, ENGINE-03, ENGINE-05, ENGINE-06, ENGINE-07]

must_haves:
  truths:
    - "The celebration animation actually plays on player finish/game over — App.css is imported and the modal's CSS classes are backed by real keyframes (ENGINE-06)"
    - "Ready/pick-up-pile/play-cards buttons dispatch moves through GameContext's dispatchMove — no direct updateGameState call for any gameplay move"
    - "An invalid play or an empty-pile pickup attempt shows a toast (via applyMove's rejection reaching GameContext's showToast) rather than a blocking alert()"
    - "GameScreen renders the board and clicking a playable card triggers a selection-state change (D-12 — real interaction, not just mount)"
    - "GameScreen's Ready button and Pick-Up-Pile button (including the confirmation-modal branch) are each exercised by an automated interaction test, not just verified via static grep (D-12)"
  artifacts:
    - path: "src/screens/GameScreen.tsx"
      provides: "full game screen: header, rules panel, Table/Hand composition, piles, ready/play/pickup controls, pickup-confirmation and celebration modals, draw-animation ghost portal, test-mode console panel — extracted from App.tsx:1065-1498"
    - path: "src/main.tsx"
      provides: "App.css import so celebration keyframes are active"
    - path: "src/__tests__/screens/GameScreen.test.tsx"
      provides: "D-12 smoke + interaction test: renders the board, clicking a selectable card updates selection state, clicking Ready dispatches READY_UP, clicking Pick Up Pile dispatches PICK_UP_PILE (both the direct-dispatch and confirmation-modal branches)"
  key_links:
    - from: "src/screens/GameScreen.tsx"
      to: "src/context/GameContext.tsx"
      via: "useGameContext() for gameState/dispatchMove/currentPlayerId/testMode/controllingPlayer/setControllingPlayer/setGameState"
      pattern: "useGameContext\\(\\)"
    - from: "src/screens/GameScreen.tsx"
      to: "src/gameLogic.ts"
      via: "GameLogic.shouldConfirmPickUp (UI pre-check per Open Question 2) and GameLogic.getCardsToDrawCount (animation prediction)"
      pattern: "GameLogic\\.(shouldConfirmPickUp|getCardsToDrawCount)"
---

<objective>
Extract the full game screen (`App.tsx:1065-1498`) into `src/screens/GameScreen.tsx`, wiring every gameplay control (Ready, Play, Pick Up Pile) through `dispatchMove` instead of direct state mutation, preserving the celebration-modal derived-state effect, the pickup-confirmation flow (per RESEARCH.md Open Question 2: `shouldConfirmPickUp` stays a UI pre-check, `applyMove`'s `PICK_UP_PILE` performs the pickup unconditionally), and the draw-card animation choreography as closely as the new single-shot `dispatchMove` model allows. Fix the missing `App.css` import (ENGINE-06).

Purpose: This is ENGINE-03's split completed, ENGINE-05's remaining gameplay alerts converted to toasts, and ENGINE-06's one-line fix finally wired up. `Table.tsx`/`Hand.tsx` continue to receive the same props they do today in this plan (via adapter functions) — Plan 01-07 is where their internal duplication is removed; this plan's job is the screen extraction itself.
Output: `src/screens/GameScreen.tsx` (replaces Plan 01-05's placeholder), `src/main.tsx` with the `App.css` import, a D-12 interaction smoke test covering card selection, Ready, and Pick-Up-Pile (both branches).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rules-engine-refactor/01-CONTEXT.md
@.planning/phases/01-rules-engine-refactor/01-RESEARCH.md
@.planning/phases/01-rules-engine-refactor/01-PATTERNS.md
@src/context/GameContext.tsx
@src/engine/moves.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameScreen shell — header, rules panel, ready/pickup wiring, celebration, ENGINE-06 fix</name>
  <files>src/screens/GameScreen.tsx, src/main.tsx</files>
  <read_first>
    src/App.tsx lines 80-149 (celebration derived-state effect and its refs), lines 355-386 (console.log interception effect), lines 836-869 (pickUpPile — the shouldConfirmPickUp pre-check flow), lines 1065-1238 (header, rules panel JSX), lines 1300-1399 (ready button, player grid, console panel), lines 1421-1495 (pickup-confirmation and celebration modal JSX)
    src/context/GameContext.tsx (dispatchMove/gameState/currentPlayerId/testMode/controllingPlayer/setControllingPlayer — exact fields to read via useGameContext())
    src/gameLogic.ts (shouldConfirmPickUp signature)
    src/main.tsx (current imports — missing `./App.css`)
  </read_first>
  <action>
    In `src/main.tsx`, add `import './App.css';` alongside the existing `import './index.css';` (ENGINE-06 — this is the entire fix; `App.tsx`'s celebration JSX already references `celebration-modal`/`celebration-emoji-pulse`/`celebration-emoji` classes whose keyframes already exist in `App.css:81-91`, per RESEARCH.md Pitfall 5).
    Create `src/screens/GameScreen.tsx`, replacing Plan 01-05's placeholder, exporting `GameScreen()`. Read `gameState`, `dispatchMove`, `currentPlayerId`, `playerId`, `testMode`, `controllingPlayer`, `setControllingPlayer` from `useGameContext()`. Local state (own to this component, matching RESEARCH.md's anti-pattern guidance that Context must not own DOM/animation state): `showRules` (boolean), `pickUpConfirmation` (`{show:boolean; playerIndex:number}|null`), `celebrationModal` (same shape as `App.tsx:73`), `consoleLogs` (`string[]`), plus the four celebration refs (`celebrationTimeoutRef`, `celebrationInitializedRef`, `celebratedGameOverRef`, `celebratedPlayerIdsRef`) and the selection state via `useSelection()` and sort mode via `useHandSorting('original')`.
    Port the celebration derived-state effect (`App.tsx:103-143`) verbatim, reading `gameState` from context instead of local state. Do not port `resetCelebration` as an externally-callable function or call it anywhere — since `GameScreen` mounts fresh each time routing switches from Menu/Lobby to Game (per Plan 01-05's phase-based routing, no more manual reset calls are needed; the refs' `useRef` initial values already start "unset" on every fresh mount, which is what `resetCelebration` used to force manually). Keep `dismissCelebration` (the X-button handler) — it still clears the timeout and nulls `celebrationModal`.
    Port the console.log interception effect (`App.tsx:356-386`) into `GameScreen`, gated on `testMode` from context, feeding the local `consoleLogs` state.
    Wire `setReady = () => dispatchMove({ type: 'READY_UP', playerId: currentPlayerId })`.
    Wire `pickUpPile` per RESEARCH.md Open Question 2's resolution: compute `effectiveRevealed` exactly as `App.tsx:851-859` does (first-turn rank-matching check against `revealedFaceDown`), call `GameLogic.shouldConfirmPickUp(player, gameState.discardPile, effectiveRevealed)` as a pure UI-side pre-check; if true, set `pickUpConfirmation({show:true, playerIndex})`; if false, dispatch immediately: `dispatchMove({type:'PICK_UP_PILE', playerId: currentPlayerId, revealedFaceDownIndex: revealedFaceDown?.index})` then clear `selectedCards`/`revealedFaceDown`. The confirmation modal's "Pick Up Anyway" button dispatches the same `PICK_UP_PILE` move and clears `pickUpConfirmation`. Do not duplicate the `discardPile.length === 0` check as a manual alert — the Pick Up Pile button stays disabled when the pile is empty (existing `disabled={gameState.discardPile.length === 0 || !isMyTurn}` condition), and `applyMove`'s `PILE_EMPTY` rejection is the backstop if that's ever bypassed (D-02's "independent of the caller" guarantee, already covered by Plan 01-02/01-03's threat register).
    Port the header (`App.tsx:1073-1145`), rules panel (`App.tsx:1148-1238`), player summary grid (`App.tsx:1345-1386`), and console output panel (`App.tsx:1388-1399`) JSX into `GameScreen`'s return, reading `gameState`/`testMode`/`controllingPlayer`/`setControllingPlayer` from context instead of props. Port the pickup-confirmation modal (`App.tsx:1421-1454`) and celebration modal (`App.tsx:1456-1495`) JSX, wiring the confirmation modal's buttons as described above.
    Leave the Table/Hand composition, the Play button, and the draw-animation portal as placeholders in this task (`{/* Task 2: Table/Hand composition + playCards wiring */}`) — Task 2 completes them in the same file.
  </action>
  <acceptance_criteria>
    - `src/main.tsx` contains `import './App.css';`
    - `GameScreen.tsx` contains zero `alert(` calls for the ready/pickup paths (grep -c returns 0 for those code paths specifically — the Play button's alerts are Task 2's job)
    - `setReady`'s body contains exactly one `dispatchMove(` call with `type: 'READY_UP'`
    - `pickUpPile`'s body calls `GameLogic.shouldConfirmPickUp` before any `dispatchMove` call
    - `npx tsc --noEmit` exits 0 (GameScreen.tsx may reference Table/Hand with placeholder/stub JSX at this point — do not import Table/Hand yet if doing so would fail typecheck before Task 2 wires their props correctly)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>GameScreen's shell, celebration, rules panel, and ready/pickup wiring are complete and dispatch through GameContext; App.css is imported so celebration keyframes are live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table/Hand composition, playCards wiring, and Ready/Pick-Up-Pile interaction tests (D-12)</name>
  <files>src/screens/GameScreen.tsx, src/__tests__/screens/GameScreen.test.tsx</files>
  <read_first>
    src/App.tsx lines 461-834 (playCards — the source of the animation choreography and dispatch point), lines 1240-1343 (Table/Hand composition and Play button JSX), lines 763-825 (the flushSync/setTimeout ghost-animation sequence), lines 1402-1417 (the draw-card-ghost portal)
    src/components/Table.tsx and src/components/Hand.tsx (current prop interfaces — GameScreen must pass matching adapter props in this plan; Plan 01-07 changes these interfaces)
    .planning/phases/01-rules-engine-refactor/01-RESEARCH.md Pitfall 4 (draw-animation fragility, accepted as a manual-QA item)
  </read_first>
  <behavior>
    - GameScreen renders `<Table>` and `<Hand>` when `currentPlayer` exists, passing `gameState`, `currentPlayerId`, `selectedCards`/`setSelectedCards` from `useSelection()`.
    - Clicking a selectable hand card (during setup phase, e.g.) results in `selectedCards` containing that card's selection — assert via a resulting DOM change (e.g. the card's `selected`-state ring class) rather than a mock, per RTL convention.
    - Clicking "Play" with a valid selection calls `dispatchMove` with `type: 'PLAY_CARDS'` and the correct `cards` array.
    - Clicking the "Ready" button in `'setup'` phase, for a player who is not yet ready, results in a `dispatchMove` call with `type: 'READY_UP'` and `playerId` equal to `currentPlayerId` — assert via an observable outcome (the resulting `gameState.players[i].isReady === true`, reachable through a test-exposed context consumer, or a DOM change such as the Ready button becoming disabled/relabelled) rather than a bare mock.
    - Clicking "Pick Up Pile" when `GameLogic.shouldConfirmPickUp` would return `false` for the seeded fixture (no rank-share with the pile, no first-turn reveal) dispatches `PICK_UP_PILE` directly, with no confirmation modal appearing.
    - Clicking "Pick Up Pile" when `GameLogic.shouldConfirmPickUp` would return `true` for the seeded fixture opens the confirmation modal instead of dispatching immediately; clicking the modal's "Pick Up Anyway" button then dispatches `PICK_UP_PILE` and closes the modal.
  </behavior>
  <action>
    Replace Task 1's Table/Hand placeholder with the real composition, porting `App.tsx:1240-1343`'s JSX. Pass `Table`/`Hand` the adapter props matching their *current* interface exactly (Plan 01-07 changes this interface, not this plan):
    - `swapCards={(handIndex, faceUpIndex) => dispatchMove({type:'SWAP_CARDS', playerId: currentPlayerId, sourceA:'hand', indexA:handIndex, sourceB:'faceUp', indexB:faceUpIndex})}`
    - `updateGameState={(newState) => setGameState(newState)}` — read `setGameState` from context. This adapter exists only because `Hand.tsx`/`Table.tsx` still contain their own hand↔hand/faceUp↔faceUp swap bypass logic that calls this prop directly (RESEARCH.md Pattern 3 site 4) — Plan 01-07 removes the need for this prop entirely by routing those swaps through `dispatchMove`'s `SWAP_CARDS` case instead. Note this explicitly with a `// TODO(01-07): remove once Hand/Table dispatch SWAP_CARDS directly` comment above the prop.
    - `gameState`, `currentPlayerId`, `currentPlayer`, `isSetupPhase`, `isMyTurn`, `selectedCards`, `setSelectedCards`, `revealedFaceDown`, `setRevealedFaceDown`, `handSortMode`, `setHandSortMode`, `drawingCards` — passed through as today.
    Implement `playCards`: guard on `player`/`gameState.phase==='playing'`/turn ownership (mirroring `App.tsx:462-469`, though `applyMove` re-validates these regardless — this guard just avoids building a useless move object). Build `selections` exactly as `App.tsx:479-481` does. When `cardSource==='hand' && handSortMode !== 'original'`: compute `sorted = GameLogic.sortHand(player.hand, handSortMode)`, `reorderedHand = sorted.map(s => s.card)`, remap every `hand`-type entry in `selections` from its old index to `sorted.findIndex(s => s.arrayIndex === oldIndex)`, call `setHandSortMode('original')` (mirrors `App.tsx:553-602`). Handle the mixed hand+faceUp validation client-side exactly as `App.tsx:485-515` did, purely to decide whether to proceed with the animation path or dispatch-and-let-the-toast-handle-it — since `applyMove` re-validates everything authoritatively regardless (rejecting with `INVALID_COMBINATION` if this pre-check was somehow wrong), this client-side check does not need to be airtight.
    For the draw-animation: predict `cardsToDraw = GameLogic.getCardsToDrawCount(player, gameState.deck.length)` and `drawnCards = gameState.deck.slice(0, cardsToDraw)` from the pre-dispatch state purely to drive the ghost-card portal (`App.tsx:1402-1417`, unchanged) for up to 700ms. Dispatch `dispatchMove({type:'PLAY_CARDS', playerId: currentPlayerId, cards: selections, reorderedHand})` immediately (do not delay the actual dispatch behind the animation timers — `applyMove` computes the authoritative final state, including drawn cards, in one shot; there is no second delayed commit like the original two-phase `flushSync` + `setTimeout(...,700)` `updateGameState` calls). Accept that the real hand will show newly-drawn cards immediately rather than being visually hidden until the ghost animation lands — this is a known, minor visual regression flagged by RESEARCH.md Pitfall 4 as acceptable for Phase 1 (POLISH-01 in Phase 6 rebuilds this animation properly on the engine); note it in this task's SUMMARY for manual QA per `01-VALIDATION.md`'s Manual-Only Verifications table. Clear `selectedCards`/`revealedFaceDown` after dispatching, matching today's behavior.
    Write `src/__tests__/screens/GameScreen.test.tsx` per the `<behavior>` spec: render `GameScreen` inside `GameProvider`, seed a `'setup'`-phase `gameState` via the provider (or a small test harness component that calls `setGameState` before rendering `GameScreen`), assert the board (`Table`/`Hand`) renders, click a selectable card, assert the resulting selection-state DOM change; separately seed a `'playing'`-phase state where the current player has a legally playable card selected, click "Play", and assert (via a spy on `window.storage.set` or by re-reading `gameState` from a test-exposed context consumer) that the move was dispatched and processed.
    Extend the same test file with two more scenarios per the `<behavior>` spec: (1) a `'setup'`-phase fixture where the current player is not ready — click the Ready button and assert `READY_UP` was dispatched (via the resulting `isReady:true` observable, same test-exposed-context or DOM-change convention used for the Play-button test); (2) two `'playing'`-phase fixtures for Pick Up Pile — one where the seeded `discardPile`/`revealedFaceDown` combination makes `GameLogic.shouldConfirmPickUp` return `false` (assert `PICK_UP_PILE` dispatches immediately, no modal appears), and one where it returns `true` (assert the confirmation modal appears first, then clicking "Pick Up Anyway" dispatches `PICK_UP_PILE`).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screens/GameScreen.test.tsx` exits 0, including the Play-button, Ready-button, and both Pick-Up-Pile (direct-dispatch and confirmation-modal) test cases
    - `npx tsc --noEmit` exits 0
    - `playCards`'s body contains exactly one `dispatchMove(` call with `type: 'PLAY_CARDS'`
    - Grep confirms no remaining `alert(` calls anywhere in `GameScreen.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screens/GameScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>GameScreen fully composes Table/Hand, dispatches PLAY_CARDS/READY_UP/PICK_UP_PILE through GameContext, and the draw-animation ghost portal still plays (with the documented, Pitfall-4-accepted timing change) — proven by D-12 interaction tests for card selection, Play, Ready, and both Pick-Up-Pile branches, not just a render-without-crashing smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GameScreen's client-side pre-checks (shouldConfirmPickUp, mixed-source validation) -> applyMove | These pre-checks are advisory UX only (deciding whether to show a confirmation modal, or whether to bother animating) — they carry no authority. A client that skipped or lied about these checks would still have every move re-validated by `applyMove` (Plan 01-02/01-03's threat register already covers the authoritative checks). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-11 | Tampering | `GameScreen`'s client-side `shouldConfirmPickUp`/mixed-source pre-checks | accept | These are UX conveniences, not security boundaries — even a modified client that always skips the confirmation modal or miscomputes the mixed-source check cannot force an illegal `PICK_UP_PILE`/`PLAY_CARDS` through, since `applyMove` re-validates every field unconditionally (already mitigated in Plan 01-02/01-03, T-01-01/T-01-02/T-01-04/T-01-05). |
</threat_model>

<verification>
`npx tsc --noEmit` exits 0. `npx vitest run src/__tests__/screens/GameScreen.test.tsx` exits 0. Manually verify in the browser (per `01-VALIDATION.md`'s Manual-Only Verifications table): the celebration modal's bounce/pulse/fade animations actually render on a finish, and the draw-card ghost animation still plays without crashing (positioning may differ slightly from pre-refactor per the accepted Pitfall 4 tradeoff). Run `npm test -- --run` for the full-suite regression check.
</verification>

<success_criteria>
GameScreen is fully extracted, every gameplay control dispatches through GameContext, the celebration animation is wired up (ENGINE-06), and D-12 interaction tests (not just render-without-crashing) prove the split didn't break wiring for card selection, Play, Ready, and both Pick-Up-Pile branches (direct-dispatch and confirmation-modal).
</success_criteria>

<output>
Create `.planning/phases/01-rules-engine-refactor/01-06-SUMMARY.md` when done
</output>
