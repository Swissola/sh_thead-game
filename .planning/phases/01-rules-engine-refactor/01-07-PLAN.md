---
phase: 01-rules-engine-refactor
plan: 7
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - src/components/Hand.tsx
  - src/components/Table.tsx
  - src/screens/GameScreen.tsx
  - src/__tests__/screens/GameScreen.test.tsx
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-04, ENGINE-07]

must_haves:
  truths:
    - "Hand.tsx and Table.tsx contain no inline hand-sort comparator or rank-share check — both call gameLogic.ts's sortHand/canAddToSelection (ENGINE-04 fully closed for these two files)"
    - "Every card-swap interaction in Hand.tsx/Table.tsx (hand<->faceUp, hand<->hand, faceUp<->faceUp) dispatches a SWAP_CARDS move through GameContext — none calls updateGameState directly (ENGINE-01's fourth duplication site closed)"
    - "Hand.tsx and Table.tsx no longer accept swapCards or updateGameState as props — they read dispatchMove/currentPlayerId from useGameContext() directly"
    - "GameScreen's setup-phase swap interactions (hand-hand, hand-faceUp, faceUp-faceUp) are covered by an automated interaction test, not just the play-a-card test from Plan 01-06"
  artifacts:
    - path: "src/components/Hand.tsx"
      provides: "hand rendering with sortHand/canAddToSelection dedup and dispatchMove-based swapping"
    - path: "src/components/Table.tsx"
      provides: "table rendering with canAddToSelection dedup and dispatchMove-based swapping"
    - path: "src/__tests__/screens/GameScreen.test.tsx"
      provides: "extended with setup-phase swap interaction coverage"
  key_links:
    - from: "src/components/Hand.tsx"
      to: "src/context/GameContext.tsx"
      via: "useGameContext() for dispatchMove/currentPlayerId, replacing the swapCards/updateGameState props"
      pattern: "useGameContext\\(\\)"
    - from: "src/components/Table.tsx"
      to: "src/gameLogic.ts"
      via: "GameLogic.canAddToSelection replacing inline rank-share checks"
      pattern: "GameLogic\\.canAddToSelection"
---

<objective>
Close out ENGINE-01 and ENGINE-04 completely by rewiring `Hand.tsx` and `Table.tsx`'s remaining duplicated logic: the hand-sort comparator, the four inline "shares a rank" checks, and — most importantly — the fourth rule-duplication site RESEARCH.md identified (`Hand.tsx:203-220`/`Table.tsx:168-185`'s hand↔hand and faceUp↔faceUp swap logic that calls `updateGameState` directly, bypassing `applyMove` entirely).

Purpose: Until this plan, ENGINE-01's "no duplicate rule logic left elsewhere" and ENGINE-02's "no move handler mutates state in place" are true for every path that goes through `dispatchMove`, but Hand.tsx/Table.tsx's own swap-reordering code still writes state directly. This is the last plan in the phase specifically because it depends on `GameScreen`'s composition (Plan 01-06) already passing the adapter props these two files currently rely on — removing that adapter is this plan's job.
Output: `Hand.tsx`/`Table.tsx` with zero inline rule duplication and zero direct state writes; `GameScreen.tsx`'s now-unnecessary adapter props removed; extended interaction test coverage for all three swap combinations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rules-engine-refactor/01-CONTEXT.md
@.planning/phases/01-rules-engine-refactor/01-RESEARCH.md
@.planning/phases/01-rules-engine-refactor/01-PATTERNS.md
@src/context/GameContext.tsx
@src/engine/moves.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire Hand.tsx and Table.tsx — dedup + SWAP_CARDS dispatch (ENGINE-01, ENGINE-02, ENGINE-04)</name>
  <files>src/components/Hand.tsx, src/components/Table.tsx</files>
  <read_first>
    src/components/Hand.tsx (whole file — this task rewrites the sort block at lines 85-108, the tooltip/selectable rank-share checks at lines 150-155 and 182-187, and the onClick swap logic at lines 195-223)
    src/components/Table.tsx (whole file — this task rewrites the rank-share checks at lines 100-109 and 131-136, and the onClick swap logic at lines 159-188)
    src/gameLogic.ts (sortHand, canAddToSelection — exact signatures from Plan 01-01)
    src/engine/moves.ts (SWAP_CARDS's sourceA/indexA/sourceB/indexB fields)
    src/context/GameContext.tsx (dispatchMove/currentPlayerId — the fields these components now read directly)
  </read_first>
  <behavior>
    - Selecting two hand cards during setup swaps their positions via one `SWAP_CARDS` dispatch with `sourceA:'hand', sourceB:'hand'` — no direct `updateGameState`/`setGameState` call from within `Hand.tsx`.
    - Selecting a hand card then a face-up card (or vice versa) during setup dispatches `SWAP_CARDS` with `sourceA:'hand', sourceB:'faceUp'`.
    - Selecting two face-up cards during setup dispatches `SWAP_CARDS` with `sourceA:'faceUp', sourceB:'faceUp'` from `Table.tsx` — no direct `updateGameState`/`setGameState` call from within `Table.tsx`.
    - A hand card's "not playable" tooltip/selectability during a rank-multi-select still reflects "must match the already-selected rank" — behaviorally identical to today, now sourced from `GameLogic.canAddToSelection`.
  </behavior>
  <action>
    In `Hand.tsx`: remove the `swapCards`/`updateGameState`/`currentPlayerId` props from `HandProps` and the destructured parameter list; add `const { dispatchMove, currentPlayerId } = useGameContext();` inside the component (import `useGameContext` from `'../context/GameContext'`).
    Replace the inline sort block (current lines 85-108: the `cardsWithIndices`/`sortedCards` construction and manual rank/suit comparator) with `const sortedCards = GameLogic.sortHand(player.hand, handSortMode);` — this single call replaces both the filtering and the sorting, since `sortHand` already returns `{card, arrayIndex}[]` in the requested order.
    Replace the two inline "shares a rank" checks (current lines 150-155 tooltip, 182-187 selectable) with calls to `GameLogic.canAddToSelection(item.card, resolvedHandSelection)`, where `resolvedHandSelection = selectedCards.filter(s => s.type === 'hand').map(s => player.hand[s.index]).filter((c): c is CardType => c !== null)`. Tooltip becomes `if (selectedCards.length > 0 && !GameLogic.canAddToSelection(item.card, resolvedHandSelection)) tooltip = 'Select same rank to play together';`; selectable's `isPlayable` gets the equivalent `if (!GameLogic.canAddToSelection(item.card, resolvedHandSelection)) isPlayable = false;`.
    Replace the onClick swap logic (current lines 195-223): when `selectedCards.length === 1 && selectedCards[0].type === 'faceUp'`, dispatch `dispatchMove({type:'SWAP_CARDS', playerId: currentPlayerId, sourceA:'hand', indexA:item.arrayIndex, sourceB:'faceUp', indexB:selectedCards[0].index})` (replacing the old `swapCards(item.arrayIndex, selectedCards[0].index)` prop call). When `selectedCards.length === 1 && selectedCards[0].type === 'hand'`, dispatch `dispatchMove({type:'SWAP_CARDS', playerId: currentPlayerId, sourceA:'hand', indexA:selectedCards[0].index, sourceB:'hand', indexB:item.arrayIndex})` (replacing the old inline `newHand`/`updatedPlayers`/`updateGameState` block entirely — delete that block, do not keep it as dead code). Both branches then call `setSelectedCards([])`.
    In `Table.tsx`: remove the `swapCards`/`updateGameState` props from `TableProps` and the destructured parameter list (`currentPlayerId` stays a prop here since `Table` also needs it for non-swap purposes — or read it from `useGameContext()` too for consistency with `Hand.tsx`; pick one and apply it uniformly, noting the choice in this task's SUMMARY). Add `const { dispatchMove } = useGameContext();` (plus `currentPlayerId` if sourced from context).
    Replace the two inline rank-share checks (current lines 100-109, 131-136) with `GameLogic.canAddToSelection(card, resolvedFaceUpSelection)`, where `resolvedFaceUpSelection = selectedCards.filter(s => s.type === 'faceUp').map(s => currentPlayer.faceUp[s.index]).filter((c): c is Card => c !== null)`.
    Replace the onClick swap logic (current lines 159-188): when `selectedCards.length === 1 && selectedCards[0].type === 'hand'`, dispatch `dispatchMove({type:'SWAP_CARDS', playerId: currentPlayerId, sourceA:'hand', indexA:selectedCards[0].index, sourceB:'faceUp', indexB:i})` (replacing the old `swapCards(selectedCards[0].index, i)` call). When `selectedCards.length === 1 && selectedCards[0].type === 'faceUp'`, dispatch `dispatchMove({type:'SWAP_CARDS', playerId: currentPlayerId, sourceA:'faceUp', indexA:selectedCards[0].index, sourceB:'faceUp', indexB:i})` (replacing the old inline `newFaceUp`/`updatedPlayers`/`updateGameState` block — delete it entirely). Both branches then call `setSelectedCards([])`.
    Leave the mixed hand+faceUp combine-selection logic in `Table.tsx` (current lines 206-226, the `canCombineWithHand` selection-building path) untouched — that path only ever calls `setSelectedCards(...)`, it never writes game state directly, so it is not one of the four duplication sites and is out of this plan's scope.
  </action>
  <acceptance_criteria>
    - `grep -c "updateGameState(" src/components/Hand.tsx src/components/Table.tsx` returns 0 for both files
    - `grep -c "swapCards" src/components/Hand.tsx src/components/Table.tsx` returns 0 for both files (prop removed, no remaining references)
    - `grep -c "dispatchMove({type:'SWAP_CARDS'" src/components/Hand.tsx` returns 2 (hand-faceUp and hand-hand) and the equivalent in `Table.tsx` returns 2 (hand-faceUp and faceUp-faceUp)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hand.tsx and Table.tsx contain zero inline rule duplication and zero direct state writes — every swap and every rank-share check routes through gameLogic.ts/applyMove.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update GameScreen's prop-passing and close out ENGINE-07 test coverage</name>
  <files>src/screens/GameScreen.tsx, src/__tests__/screens/GameScreen.test.tsx</files>
  <read_first>
    src/screens/GameScreen.tsx (the `swapCards`/`updateGameState` adapter props passed to Table/Hand from Plan 01-06's Task 2, marked with a `TODO(01-07)` comment)
    src/components/Hand.tsx, src/components/Table.tsx (their new prop interfaces from Task 1)
    src/__tests__/screens/GameScreen.test.tsx (the existing test from Plan 01-06 to extend, not replace)
  </read_first>
  <behavior>
    - Rendering `GameScreen` in `'setup'` phase, clicking a hand card then a face-up card, results in the two cards' positions swapping in the rendered output (or, equivalently, in the underlying `gameState` reachable via the test's `GameProvider` — assert whichever is more directly observable via RTL).
    - The same for hand<->hand and faceUp<->faceUp click sequences.
  </behavior>
  <action>
    In `GameScreen.tsx`, remove the `swapCards`/`updateGameState` props (and the `// TODO(01-07)` comment) from the `<Table>`/`<Hand>` JSX invocations — `Table`/`Hand` no longer accept them per Task 1's interface change. Keep passing the props they still need (`gameState`, `currentPlayerId` if `Table` still takes it as a prop per Task 1's noted choice, `selectedCards`/`setSelectedCards`, `revealedFaceDown`/`setRevealedFaceDown`, `handSortMode`/`setHandSortMode`, `drawingCards`).
    Extend `src/__tests__/screens/GameScreen.test.tsx` (do not replace Plan 01-06's existing play-card test) with three new `it(...)` blocks per the `<behavior>` spec above: hand-hand swap, hand-faceUp swap, faceUp-faceUp swap, each seeding a `'setup'`-phase `gameState` with known card contents at specific indices and asserting the post-swap arrangement.
    Run the full suite once as a final regression pass for this phase: `npm test -- --run`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screens/GameScreen.test.tsx` exits 0, including the three new swap tests
    - `npx tsc --noEmit` exits 0
    - `npm test -- --run` (the complete phase test suite: engine, gameLogic, screens, hooks, utils) exits 0
    - Grep confirms zero remaining `TODO(01-07)` markers anywhere in `src/`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screens/GameScreen.test.tsx && npx tsc --noEmit && npm test -- --run</automated>
  </verify>
  <done>GameScreen's prop-passing matches Hand/Table's final interface; all three setup-phase swap paths are interaction-tested; the full phase test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hand.tsx/Table.tsx click handlers -> dispatchMove | Identical boundary to GameScreen's (Plan 01-06) — these components now dispatch `SWAP_CARDS` moves that `applyMove` (Plan 01-02) fully re-validates (bounds-checking, phase-checking) regardless of what the UI believes is selected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-12 | Tampering | `Hand.tsx`/`Table.tsx`'s resolved-selection arrays passed to `GameLogic.canAddToSelection` | accept | These are presentation-only tooltip/selectability computations, not move payloads — an incorrect result here at worst shows a wrong tooltip or lets the UI attempt an ultimately-rejected selection; `applyMove`'s own validation (already mitigated) is what actually enforces game rules. |
</threat_model>

<verification>
`npx tsc --noEmit` exits 0. `npx vitest run src/__tests__/screens/GameScreen.test.tsx` exits 0. `npm test -- --run` (full phase suite: engine, gameLogic, screens, hooks, utils, testUtils) exits 0 — this is the phase's final regression gate before `/gsd-verify-work`.
</verification>

<success_criteria>
Every one of the four rule-duplication sites RESEARCH.md identified (hand-sort, rank-share x4, deck-creation, and the hand↔hand/faceUp↔faceUp swap bypass) is closed. Hand.tsx and Table.tsx contain no direct state writes. The full phase test suite is green, satisfying ENGINE-07's "automated test coverage for the engine and split screens" for the whole phase.
</success_criteria>

<output>
Create `.planning/phases/01-rules-engine-refactor/01-07-SUMMARY.md` when done
</output>
