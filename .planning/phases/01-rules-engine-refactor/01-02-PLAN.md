---
phase: 01-rules-engine-refactor
plan: 2
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/engine/applyMove.ts
  - src/__tests__/engine/applyMove.test.ts
autonomous: true
requirements: [ENGINE-01, ENGINE-02, ENGINE-07]

must_haves:
  truths:
    - "applyMove rejects a READY_UP, SWAP_CARDS, or PICK_UP_PILE move for an unknown playerId, and returns the original state reference unchanged (D-02)"
    - "applyMove rejects PICK_UP_PILE from a player whose id does not match state.currentTurn's player (D-03)"
    - "A successful SWAP_CARDS (hand<->faceUp, hand<->hand, or faceUp<->faceUp), READY_UP, or PICK_UP_PILE call returns a new players array and a new player object at every array level it touches — never the same reference as the input state"
    - "The literal input state object passed into applyMove is never mutated, for any of the three implemented move types, on either the success or rejection path"
  artifacts:
    - path: "src/engine/applyMove.ts"
      provides: "applyMove(state, move) -> {state, error?} with READY_UP, SWAP_CARDS, PICK_UP_PILE cases fully implemented and a PLAY_CARDS stub"
      exports: ["applyMove"]
    - path: "src/__tests__/engine/applyMove.test.ts"
      provides: "exhaustive per-move-type tests (valid case + every distinct rejection reason) for READY_UP/SWAP_CARDS/PICK_UP_PILE, per D-11"
  key_links:
    - from: "src/engine/applyMove.ts"
      to: "src/gameLogic.ts"
      via: "GameLogic.getStartingPlayer / GameLogic.getNextPlayer calls for phase-transition and turn-advance decisions"
      pattern: "GameLogic\\.(getStartingPlayer|getNextPlayer)"
    - from: "src/engine/applyMove.ts"
      to: "src/engine/moves.ts"
      via: "Move / ApplyMoveResult / MoveError type imports"
      pattern: "from ['\"]\\./moves['\"]"
---

<objective>
Build the pure `applyMove(state, move) -> {state, error?}` reducer's `READY_UP`, `SWAP_CARDS`, and `PICK_UP_PILE` cases — the three structurally simpler move types — with exhaustive D-11 test coverage (valid case plus every distinct rejection reason for each). `PLAY_CARDS` (the most complex case: burn/draw/win sequencing, blind plays, mixed-source plays) is deliberately deferred to Plan 01-03, which extends the same file.

Purpose: This reducer is explicitly "the actual Phase 2 security boundary" (D-02) — every rule decision must route through `gameLogic.ts` predicates, every array touched must be freshly copied (ENGINE-02), and every rejection must return the *original* state reference unchanged. Building this test-first, exhaustively, is what makes Phase 2's server-side reuse trustworthy.
Output: `src/engine/applyMove.ts` with three of four move-type cases implemented and tested; a `PLAY_CARDS` stub case that returns a clearly-temporary rejection so the switch compiles and Plan 01-03 has an obvious extension point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-rules-engine-refactor/01-CONTEXT.md
@.planning/phases/01-rules-engine-refactor/01-RESEARCH.md
@src/engine/moves.ts
@src/engine/errors.ts
@src/__tests__/testUtils/buildGameState.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write exhaustive failing tests for READY_UP, SWAP_CARDS, PICK_UP_PILE</name>
  <files>src/__tests__/engine/applyMove.test.ts</files>
  <read_first>
    src/engine/moves.ts (Move union field names — sourceA/indexA/sourceB/indexB for SWAP_CARDS, revealedFaceDownIndex for PICK_UP_PILE)
    src/engine/errors.ts (the nine ERROR_CODES to assert against)
    src/__tests__/testUtils/buildGameState.ts (buildGameState/buildPlayer/buildCard signatures)
    src/App.tsx lines 388-420 (swapCards), 422-452 (setReady), 836-924 (pickUpPile/confirmPickUpPile) — the behavior being ported, for valid-case expectations
    src/__tests__/gameLogic/core.test.ts (describe/it/expect grouping convention to mirror: one describe per move type)
  </read_first>
  <behavior>
    - `describe('applyMove - READY_UP')`: valid call sets `isReady:true` for the calling player only; valid call where all players are now ready transitions `phase` to `'playing'`, sets `currentTurn` via `GameLogic.getStartingPlayer`, sets `isFirstTurn:true`; rejects `UNKNOWN_PLAYER` for a playerId not in `state.players`; rejects `WRONG_PHASE` when `state.phase !== 'setup'`.
    - `describe('applyMove - SWAP_CARDS')`: valid hand<->faceUp swap exchanges the two cards and returns new `hand`/`faceUp` array references; valid hand<->hand swap (sourceA==='hand', sourceB==='hand') exchanges two hand slots via one new `hand` array reference; valid faceUp<->faceUp swap likewise; rejects `WRONG_PHASE` when `state.phase !== 'setup'`; rejects `INVALID_SELECTION` when `indexA`/`indexB` is out of bounds; rejects `INVALID_SELECTION` when either resolved slot is `null`; rejects `UNKNOWN_PLAYER`.
    - `describe('applyMove - PICK_UP_PILE')`: valid call moves every `discardPile` card into the caller's `hand` (filling `null` slots first, then extending), clears `discardPile`, advances `currentTurn` via `GameLogic.getNextPlayer`; valid call with `revealedFaceDownIndex` set also unshifts that face-down card into the pickup and nulls it out of `faceDown`; rejects `PILE_EMPTY` when `discardPile.length === 0`; rejects `NOT_YOUR_TURN` when `move.playerId` isn't `state.players[state.currentTurn].id`; rejects `WRONG_PHASE` when `state.phase !== 'playing'`; rejects `UNKNOWN_PLAYER`.
    - Every single test in all three describe blocks (valid and rejection alike) additionally asserts the D-11/ENGINE-02 pair: `expect(result.state).not.toBe(inputState)` on success (or `expect(result.state).toBe(inputState)` on rejection, per D-02), plus `expect(inputStateSnapshot).toEqual(structuredClone(inputStateBeforeCall))` proving the input object was never mutated. For SWAP_CARDS/PICK_UP_PILE valid cases, also assert `result.state.players).not.toBe(inputState.players)` and the specific touched array (`result.state.players[i].hand`) is a new reference.
  </behavior>
  <action>
    Write the full test file per the `<behavior>` spec above, importing `applyMove` from `../../engine/applyMove` (this import will fail to resolve/compile until Task 2 creates the file — that failure IS the RED state; if the module needs to exist for TypeScript to parse the test file at all, create an empty `export function applyMove(){}` stub in Task 1 solely to make the test file parseable, and let the assertions fail at runtime instead).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/engine/applyMove.test.ts` runs (module resolves) and fails with assertion errors, not import/compile errors — confirming genuine RED state
    - Test count: at least 4 tests for READY_UP, at least 6 for SWAP_CARDS, at least 5 for PICK_UP_PILE (one per bullet above, valid cases may need 2+ tests each for sub-cases)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/engine/applyMove.test.ts</automated>
  </verify>
  <done>Exhaustive failing test suite exists for the three move types, committed before any implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement applyMove's READY_UP, SWAP_CARDS, PICK_UP_PILE cases</name>
  <files>src/engine/applyMove.ts</files>
  <read_first>
    src/__tests__/engine/applyMove.test.ts (the tests just written — the implementation target)
    src/App.tsx lines 388-420 (swapCards — already-immutable baseline pattern to generalize), 422-452 (setReady — phase-transition-on-condition pattern), 871-924 (confirmPickUpPile — the pickup logic to port, minus the animation/persistence tail)
    src/gameLogic.ts (getStartingPlayer, getNextPlayer signatures)
  </read_first>
  <action>
    Implement `src/engine/applyMove.ts`:
    - `export function applyMove(state: GameState, move: Move): ApplyMoveResult` — looks up `playerIndex = state.players.findIndex(p => p.id === move.playerId)`; if `-1`, return `{state, error:{code: ERROR_CODES.UNKNOWN_PLAYER, message:'Player not found'}}` before the switch (applies to all move types).
    - Switch on `move.type`, dispatching to `applyReadyUp`, `applySwapCards`, `applyPickUpPile`, and a temporary `applyPlayCardsStub` for `'PLAY_CARDS'` that returns `{state, error:{code: ERROR_CODES.INVALID_PLAY, message: 'PLAY_CARDS not yet implemented'}}` (Plan 01-03 replaces this function entirely — leave a `// TODO(01-03): replace with real PLAY_CARDS implementation` comment above it).
    - `applyReadyUp`: reject `WRONG_PHASE` if `state.phase !== 'setup'`. Else build `updatedPlayers = state.players.map((p,i) => i===playerIndex ? {...p, isReady:true} : p)`; if `updatedPlayers.every(p=>p.isReady)`, also set `phase:'playing'`, `currentTurn: GameLogic.getStartingPlayer(updatedPlayers)`, `isFirstTurn:true`, and an appropriate `lastAction`; else keep `phase` as `'setup'`. Return `{state: {...state, players: updatedPlayers, lastAction: ...}}`.
    - `applySwapCards`: reject `WRONG_PHASE` if `state.phase !== 'setup'`. Resolve each of `sourceA`/`sourceB` to the player's `hand` or `faceUp` array (reject `INVALID_SELECTION` for any other `CardSource` value, e.g. `'faceDown'`, since swapping isn't supported there). Bounds-check `indexA`/`indexB` against their resolved array's length and non-null-ness — reject `INVALID_SELECTION` on any failure. If `sourceA === sourceB`, copy that one array once and swap both indices within it (`arr[indexA]`, `arr[indexB]`) — do not copy the array twice or you will lose one side of the swap. If `sourceA !== sourceB`, copy both arrays, swap one element across them. Build the updated player via `{...player, [sourceA]: arrA, [sourceB]: arrB}` (or the single-array form when `sourceA===sourceB`), then `state.players.map(...)` to place it. Return the new state with an appropriate `lastAction`.
    - `applyPickUpPile`: reject `WRONG_PHASE` if `state.phase !== 'playing'`; reject `NOT_YOUR_TURN` if `move.playerId !== state.players[state.currentTurn].id` (D-03); reject `PILE_EMPTY` if `state.discardPile.length === 0`. Else port `App.tsx:871-924`'s logic exactly: copy `player.hand`, build `cardsToAdd` from `[...state.discardPile]`, if `move.revealedFaceDownIndex` is present and in-bounds, `unshift` that face-down card into `cardsToAdd` and null it out of a copied `faceDown` array; fill `null` slots in the copied hand first, then `push` any remainder; build `updatedPlayer`, `updatedPlayers`, `nextTurn = GameLogic.getNextPlayer(playerIndex, updatedPlayers)`; return `{state: {...state, players: updatedPlayers, discardPile: [], currentTurn: nextTurn, lastAction: ...}}`.
    Every helper must build every touched array/object fresh (`[...array]` before any index-assignment) — never index-assign into an array obtained via `{...player}` alone, matching the Shared Patterns section of `01-PATTERNS.md`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/engine/applyMove.test.ts` exits 0, all tests from Task 1 pass (GREEN)
    - `npx tsc --noEmit` exits 0
    - Manual source check: `applySwapCards`'s same-source branch copies exactly one array (grep confirms no `const arr = [...` appears twice for the `sourceA === sourceB` branch)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/engine/applyMove.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>READY_UP, SWAP_CARDS, PICK_UP_PILE are fully implemented and pass every D-11 test written in Task 1; PLAY_CARDS is a clearly-marked stub for Plan 01-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caller -> applyMove | `move.playerId` and all payload indices (`indexA`, `indexB`, `revealedFaceDownIndex`) are untrusted input from applyMove's perspective — today's only caller is the trusted UI, but per D-02 this boundary is enforced unconditionally so Phase 2's network caller inherits the same guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Spoofing | `applyPickUpPile` | mitigate | D-03 turn-ownership check: reject any `PICK_UP_PILE` where `move.playerId !== state.players[state.currentTurn].id`, tested explicitly (NOT_YOUR_TURN case in Task 1). |
| T-01-02 | Tampering | `applySwapCards`, `applyPickUpPile` | mitigate | Every index (`indexA`, `indexB`, `revealedFaceDownIndex`) is bounds-checked against the relevant array's length and checked for a non-null card before use; out-of-range or null-slot references return `INVALID_SELECTION` rather than producing `undefined`/`NaN` propagation into state (V5 Input Validation), tested explicitly in Task 1. |
| T-01-03 | Repudiation | `applyMove` (all cases) | accept | No audit logging in Phase 1 — `{code, message}` on every rejection is the groundwork for Phase 2 server-side logging (MPLAY-04), not itself a logging mechanism. Low risk: single-browser, no adversarial multi-party context yet. |
</threat_model>

<verification>
`npx vitest run src/__tests__/engine/applyMove.test.ts` exits 0 with the full D-11 matrix for READY_UP/SWAP_CARDS/PICK_UP_PILE green. `npx tsc --noEmit` exits 0. Run `npm test -- --run` for the full-suite regression check per the phase's sampling rate.
</verification>

<success_criteria>
`applyMove.ts` correctly implements three of four move types with no state mutation at any level, enforces D-03's turn-ownership check where applicable, and every rejection path is covered by an exhaustive D-11 test asserting both the correct error code and the unchanged-input-state guarantee.
</success_criteria>

<output>
Create `.planning/phases/01-rules-engine-refactor/01-02-SUMMARY.md` when done
</output>
