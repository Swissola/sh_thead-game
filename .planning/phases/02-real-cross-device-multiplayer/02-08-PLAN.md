---
phase: 02-real-cross-device-multiplayer
plan: 08
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/hooks/useRoomSubscription.ts
  - src/hooks/usePresence.ts
  - src/__tests__/hooks/useRoomSubscription.test.ts
  - src/__tests__/hooks/usePresence.test.ts
autonomous: true
requirements: [MPLAY-02, MPLAY-05, MPLAY-06]

must_haves:
  truths:
    - "A change made to a room by any device appears on every other device without the app polling for it"
    - "When the server's state differs from what the local player optimistically applied, the client snaps to the server's version and says so"
    - "Leaving a room or unmounting tears the subscription down, so no stale channel keeps writing state"
    - "Every player can see which opponents are currently connected"
  artifacts:
    - path: "src/hooks/useRoomSubscription.ts"
      provides: "Realtime postgres_changes subscription and reconciliation trigger"
      exports: ["useRoomSubscription"]
      min_lines: 40
    - path: "src/hooks/usePresence.ts"
      provides: "Per-room Presence channel plus heartbeat cadence"
      exports: ["usePresence"]
  key_links:
    - from: "src/hooks/useRoomSubscription.ts"
      to: "rooms table"
      via: "postgres_changes channel filtered by room_code"
      pattern: "postgres_changes"
    - from: "src/hooks/usePresence.ts"
      to: "heartbeat edge function"
      via: "interval invoke"
      pattern: "heartbeat"
---

<objective>
Replace `App.tsx`'s 2-second `localStorage` poll with a Realtime `postgres_changes`
subscription, and add the Presence channel that drives D-10's offline badges. The
subscription callback is also the reconciliation trigger for MPLAY-05 — not the
`functions.invoke()` response, because either can arrive first.

Purpose: MPLAY-02 (Realtime replaces the poll), MPLAY-05 (reconcile against the
authoritative broadcast), MPLAY-06 (see who has dropped).
Output: `useRoomSubscription` and `usePresence` plus tests. Wiring into `App.tsx` and the
screens happens in Wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-real-cross-device-multiplayer/02-RESEARCH.md
@.planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md
@.planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md
@src/supabase/roomTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRoomSubscription — Realtime state stream replacing the poll (MPLAY-02)</name>
  <files>src/hooks/useRoomSubscription.ts, src/__tests__/hooks/useRoomSubscription.test.ts</files>
  <read_first>
    - src/App.tsx (lines 23-32 — the exact poll effect being replaced: its `if (!gameState || testMode) return` guard, its `gameState?.roomCode`/`testMode`/`setGameState` dependency array, and its cleanup-on-unmount contract)
    - src/supabase/client.ts (`getSupabaseClient`)
    - src/supabase/roomTypes.ts (`RoomRow`, `ServerRoom`, `rowToServerRoom`)
    - src/hooks/useToast.ts (the `show(message, code?, variant?)` signature plan 02-02 added — the reconciliation toast uses `variant: 'reconcile'`)
    - .planning/phases/02-real-cross-device-multiplayer/02-RESEARCH.md (Architecture Patterns Pattern 1 and Pattern 3; Pitfall 7 on REPLICA IDENTITY)
    - .planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md (the `useRoomSubscription` section: shape to carry forward, and the reconciliation responsibility note)
    - src/__tests__/hooks/useToast.test.ts (renderHook and fake-timer conventions to follow)
  </read_first>
  <behavior>
    - With `testMode` true, or with an empty room code, the hook subscribes to nothing and creates no channel
    - On mount with a room code the hook creates one channel named for that room and subscribes to `postgres_changes` for `UPDATE` and `INSERT` on `public.rooms` filtered by that room code
    - A payload whose `version` is greater than the last applied version calls `onServerRoom` with the mapped `ServerRoom`
    - A payload whose `version` is less than or equal to the last applied version is ignored (out-of-order or duplicate delivery)
    - When the incoming server state differs from the locally-held state, `onReconciled` is called exactly once for that payload; when they match, `onReconciled` is not called
    - Unmounting removes the channel exactly once
    - Changing the room code removes the old channel before creating the new one
  </behavior>
  <action>
Create `src/hooks/useRoomSubscription.ts` exporting
`useRoomSubscription({ roomCode, testMode, localState, onServerRoom, onReconciled })`.

Carry the replaced poll effect's shape forward exactly: the same
`if (!roomCode || testMode) return` guard, a dependency array keyed on the room code and test
mode, and a cleanup return — only the body changes, from `setInterval` + `window.storage.get`
to `getSupabaseClient().channel(...).on('postgres_changes', { event, schema: 'public', table:
'rooms', filter: 'room_code=eq.' + roomCode }, handler).subscribe()`, cleaned up with
`removeChannel` instead of `clearInterval`. Test Mode must keep working exactly as it does today
(CLAUDE.md: Test Mode is fully client-side and never reads or writes persistence), so the
`testMode` short-circuit is load-bearing, not decoration.

Read only `payload.new` — `payload.old` is empty without `REPLICA IDENTITY FULL`, which this
phase deliberately does not set (RESEARCH.md Pitfall 7). Map the row with `rowToServerRoom`.

Track the last applied version in a ref and drop any payload at or below it, so duplicate or
out-of-order deliveries cannot rewind the board.

This hook is the reconciliation trigger for MPLAY-05, not the `functions.invoke()` response —
either can arrive first, and only the broadcast is authoritative. When a newer-version payload
arrives whose state differs from `localState`, call `onReconciled()` so the caller can raise
D-12's distinct toast; always call `onServerRoom` regardless, because the client always snaps to
server truth (D-11). Compare states with a stable serialisation of the fields that matter rather
than reference identity.

Create `src/__tests__/hooks/useRoomSubscription.test.ts` using `renderHook` with a fake Supabase
client (`vi.mock('../../supabase/client')`) whose `channel()` returns a stub recording `.on`
registrations and exposing a way to fire a payload. Cover all seven behaviours, including the
two teardown cases.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/hooks/useRoomSubscription.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven behaviours have a passing test, including the ignore-older-version case and both teardown cases
    - `grep -c 'setInterval' src/hooks/useRoomSubscription.ts` returns 0
    - `grep -c 'window.storage' src/hooks/useRoomSubscription.ts` returns 0
    - `grep -c 'removeChannel' src/hooks/useRoomSubscription.ts` is at least 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Room state streams in over Realtime with duplicate/stale payloads filtered and reconciliation signalled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usePresence — connected-player set plus heartbeat cadence (MPLAY-06, D-10)</name>
  <files>src/hooks/usePresence.ts, src/__tests__/hooks/usePresence.test.ts</files>
  <read_first>
    - src/hooks/useToast.ts (the hook shape to follow: internal state, a ref for the timer, cleanup — per PATTERNS.md this is the closest structural precedent since no Presence code exists in the repo)
    - src/supabase/roomTypes.ts (`HEARTBEAT_INTERVAL_MS`, `DISCONNECT_THRESHOLD_MS`)
    - supabase/functions/_shared/heartbeat.ts (what the heartbeat call writes server-side, so the client cadence matches the server's staleness threshold)
    - .planning/phases/02-real-cross-device-multiplayer/02-RESEARCH.md (Architecture Patterns Pattern 5 for the exact track/sync/join/leave API; the anti-pattern warning that Presence is not the D-05 timeout mechanism)
    - .planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md (the two badge states the returned data must be able to distinguish)
  </read_first>
  <behavior>
    - With `testMode` true, or with an empty room code or empty player id, the hook creates no channel and returns an empty online set
    - On `SUBSCRIBED` the hook calls `track` once with the local player id
    - A `sync` event replaces the returned online set with the channel's current presence keys
    - A `leave` event for another player removes that player from the online set
    - The hook invokes the `heartbeat` edge function immediately on subscribe and then every `HEARTBEAT_INTERVAL_MS`
    - Unmounting removes the channel and clears the heartbeat interval
    - `isPlayerOffline(playerId)` returns true for a player absent from the online set and false for a present one
  </behavior>
  <action>
Create `src/hooks/usePresence.ts` exporting `usePresence({ roomCode, playerId, testMode })`
returning `{ onlinePlayerIds: string[]; isPlayerOffline: (id: string) => boolean }`.

Follow `useToast.ts`'s hook shape — internal `useState` for the derived set, a `useRef` for the
interval handle, a `useEffect` that subscribes on mount and tears down on unmount, returning a
small plain object rather than the raw channel.

Open a channel named distinctly from the state channel (a `-presence` suffix), configured with
`presence: { key: playerId }`, and `track` on `SUBSCRIBED`. D-03 explicitly allows the same
identity in multiple tabs, so keying on the player id is correct: both tabs collapse to one
presence entry, which is exactly the desired display behaviour.

Drive the heartbeat from the same effect: invoke the `heartbeat` edge function on subscribe and
on a `HEARTBEAT_INTERVAL_MS` interval. Presence and the heartbeat are deliberately two separate
signals — Presence is ephemeral, client-reported and drives the badge; `player_seen` is
server-written and is what D-06's rejoin matching and D-08's host transfer trust. Never let a
Presence event drive a rules decision, and never let the server's staleness view drive the badge
directly. Swallow heartbeat failures silently; a missed beat is not a user-facing error, and the
next beat corrects it.

Create `src/__tests__/hooks/usePresence.test.ts` with a fake Supabase client whose `channel()`
stub can fire `sync`/`leave` and report `SUBSCRIBED`, and `vi.useFakeTimers()` for the interval
cadence. Cover all seven behaviours.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/hooks/usePresence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven behaviours have a passing test, including the interval-cadence and teardown cases
    - `grep -c 'TURN_GRACE_MS' src/hooks/usePresence.ts` returns 0 — Presence must not touch the D-05 timeout
    - `grep -c 'removeChannel' src/hooks/usePresence.ts` is at least 1
    - `npm test -- --run` (full suite) passes
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Connected players are visible to the UI, and the server keeps receiving verified liveness beats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Realtime cluster → browser | Payloads arrive unordered and can be duplicated |
| browser Presence report → other browsers | Self-reported and unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Tampering | Out-of-order or replayed Realtime payloads rewinding the board | mitigate | Last-applied version tracked in a ref; any payload at or below it is dropped |
| T-02-30 | Spoofing | A client falsely reporting itself present | accept | Presence drives display only (D-10 badges). Every rules decision — D-05 timeout, D-06 seat matching, D-08 host transfer — uses the server-written `player_seen`, never Presence. Acceptance criteria grep for the absence of `TURN_GRACE_MS` in the presence hook to keep the two apart |
| T-02-31 | Information Disclosure | Realtime channel readable by any authenticated user who knows the room code | accept | Same posture as T-02-08: the room code is the access boundary and the payload contains only what the `rooms` `select` policy already exposes |
</threat_model>

<verification>
- `npm test -- --run src/__tests__/hooks/` passes
- `grep -rn "setInterval" src/hooks/useRoomSubscription.ts` returns nothing
- Full suite and build green
</verification>

<success_criteria>
- Realtime replaces polling with correct teardown and stale-payload filtering
- Reconciliation is signalled from the broadcast, not the invoke response
- Presence drives display only; the server's liveness signal drives rules
</success_criteria>

<output>
Create `.planning/phases/02-real-cross-device-multiplayer/02-08-SUMMARY.md` when done
</output>
