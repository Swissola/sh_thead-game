---
phase: 02-real-cross-device-multiplayer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/gameLogic.ts
  - src/engine/errors.ts
  - src/engine/moves.ts
  - src/engine/applyMove.ts
  - src/__tests__/engine/applyMove.test.ts
  - src/hooks/useToast.ts
  - src/components/Toast.tsx
  - src/__tests__/hooks/useToast.test.ts
  - src/__tests__/components/Toast.test.tsx
autonomous: true
requirements: [MPLAY-04, MPLAY-05]

must_haves:
  truths:
    - "A modified client cannot convert a face-down card into a known hand card by sending PICK_UP_PILE with revealedFaceDownIndex while it still holds hand or face-up cards"
    - "The hand -> faceUp -> faceDown play-order rule (CR-01) is still enforced and covered by regression tests"
    - "The engine modules can be imported by a Deno runtime without extensionless-specifier resolution"
    - "A toast can be rendered in a reconciliation or reconnect style distinct from the ordinary error style, without breaking any existing two-argument showToast call"
  artifacts:
    - path: "src/engine/applyMove.ts"
      provides: "revealedFaceDownIndex source gate in applyPickUpPile"
      contains: "getAvailableCardSource"
    - path: "src/hooks/useToast.ts"
      provides: "ToastState.variant with a backward-compatible default"
      contains: "variant"
    - path: "src/components/Toast.tsx"
      provides: "variant-keyed border colour and icon"
  key_links:
    - from: "src/engine/applyMove.ts"
      to: "src/gameLogic.ts"
      via: "extension-qualified relative import"
      pattern: "from '\\.\\./gameLogic\\.ts'"
    - from: "src/components/Toast.tsx"
      to: "src/hooks/useToast.ts"
      via: "ToastState.variant"
      pattern: "toast\\.variant"
---

<objective>
Two pieces of purely local preparation, neither of which needs Supabase to exist:
(1) harden `applyMove` for its new life as a server trust boundary (D-13) and make the
engine modules Deno-importable, and (2) extend the toast plumbing with the `variant`
field D-10/D-12 need. Both unblock later waves; neither touches the network.

Purpose: MPLAY-04 makes `applyMove` the server's authority, so its remaining
untrusted-client gap must close and a Deno Edge Function must be able to import it
verbatim (never reimplement it). MPLAY-05's reconciliation UX needs a visually distinct
toast before the reconciliation logic that fires it exists.
Output: hardened engine with regression tests, Deno-safe import specifiers, variant-aware
toast system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md
@.planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CR-01 is closed, then gate revealedFaceDownIndex in applyPickUpPile (D-13)</name>
  <files>src/engine/applyMove.ts, src/__tests__/engine/applyMove.test.ts</files>
  <read_first>
    - src/engine/applyMove.ts (whole file — in particular the CR-01 source-validation block at lines 94-112 which is the pattern to replicate, and `applyPickUpPile` at lines 345-410 with its "deliberately NOT gated" comment)
    - src/gameLogic.ts (`getAvailableCardSource`, line 194 — the predicate the gate uses)
    - src/engine/errors.ts (the closed ERROR_CODES set — no new codes may be invented)
    - src/__tests__/engine/applyMove.test.ts (the two existing CR-01 regression tests, to extend alongside rather than duplicate)
    - .planning/phases/02-real-cross-device-multiplayer/02-RESEARCH.md (Common Pitfalls 1 and 2)
  </read_first>
  <action>
First, run `npm test -- --run src/__tests__/engine/applyMove.test.ts` and confirm the two
existing CR-01 regression tests pass. RESEARCH.md Pitfall 1 established that CR-01 itself is
already fixed (commit `a8a4ef3`) — verify, do not re-implement. Do not add a second play-order
check to `applyPlayCards`.

Then close the genuinely-open adjacent gap (RESEARCH.md Pitfall 2). In `applyPickUpPile`,
`move.revealedFaceDownIndex` is currently honoured regardless of the player's available card
source. Under Phase 1's trusted client that was an accepted risk; under MPLAY-04's server
boundary it lets a modified client turn an unseen face-down card into a known hand card while
still holding hand or face-up cards. Close it:

  - Compute `const cardSource = GameLogic.getAvailableCardSource(player);` before the
    `revealedFaceDownIndex` block.
  - When `move.revealedFaceDownIndex !== undefined` and `cardSource !== 'faceDown'`, return
    `{ state, error: { code: ERROR_CODES.INVALID_SELECTION, message: \`You must play from your ${cardSource} cards first\` } }` —
    the same code and message wording already used by the CR-01 fix, keeping the closed
    ERROR_CODES set intact (Phase 1 D-04).
  - Leave the existing bounds/null checks on the index in place; the new gate sits before them.
  - Replace the existing "deliberately NOT gated" comment block with one recording the Phase 2
    reason for the change: the client is no longer trusted, so the accepted-risk rationale no
    longer holds.
  - Do not change any other behaviour of `applyPickUpPile` — picking up an unrevealed pile is
    still always legal, which is what makes D-05's server auto-pickup safe.

Add regression tests in `src/__tests__/engine/applyMove.test.ts` in the same `describe` block
as the existing CR-01 tests, matching their structure: (a) `PICK_UP_PILE` with
`revealedFaceDownIndex` set while the player still holds hand cards is rejected with
`INVALID_SELECTION`; (b) the same while the player holds only face-up cards is rejected;
(c) the same when the player's only remaining source is `faceDown` still succeeds and moves
the revealed card into the hand. Use `src/__tests__/testUtils/buildGameState.ts` for fixtures.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/engine/applyMove.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The two pre-existing CR-01 tests still pass unmodified
    - Three new `PICK_UP_PILE` + `revealedFaceDownIndex` tests exist and pass
    - `src/engine/applyMove.ts` contains `getAvailableCardSource` inside `applyPickUpPile`
    - `src/engine/errors.ts` is unchanged (`git diff --stat src/engine/errors.ts` shows no change)
    - No new branch was added to `applyPlayCards`
  </acceptance_criteria>
  <done>The last untrusted-client gap in the engine is closed with test coverage, and CR-01's existing fix is confirmed rather than duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Make the engine modules Deno-importable with extension-qualified specifiers</name>
  <files>src/types.ts, src/gameLogic.ts, src/engine/errors.ts, src/engine/moves.ts, src/engine/applyMove.ts</files>
  <read_first>
    - src/engine/applyMove.ts (its relative imports: `../gameLogic`, `../types`, `./moves`, `./errors`)
    - src/engine/moves.ts (imports `../types`, `./errors`)
    - src/gameLogic.ts (its relative import of `./types`)
    - tsconfig.app.json (`allowImportingTsExtensions: true` and `moduleResolution: "bundler"` are already set, so explicit `.ts` specifiers are legal today)
  </read_first>
  <action>
Deno resolves module specifiers literally and will not silently append `.ts`. A later plan
creates `supabase/functions/_shared/engine.ts` re-exporting these exact modules so the Edge
Functions run the same reducer as the browser (never a reimplementation), which requires every
relative import in the transitive closure of `applyMove` to carry an explicit `.ts` extension.

Append `.ts` to every relative import specifier in exactly these five files:
`src/types.ts`, `src/gameLogic.ts`, `src/engine/errors.ts`, `src/engine/moves.ts`,
`src/engine/applyMove.ts`. Example targets: `'../gameLogic'` becomes `'../gameLogic.ts'`,
`'./errors'` becomes `'./errors.ts'`, `'../types'` becomes `'../types.ts'`.

Do not touch any other file — the rest of `src/` keeps its extensionless imports, because Vite
and Vitest resolve both forms and a repo-wide rewrite is unnecessary churn. Do not change any
package/bare specifiers (`react`, `lucide-react`). Do not add a build step or a copy script:
the Edge Functions import these files directly so there is exactly one copy of the rules.
  </action>
  <verify>
    <automated>npm run build && npm test -- --run src/__tests__/engine src/__tests__/gameLogic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from '(\./|\.\./)[^']*'" src/types.ts src/gameLogic.ts src/engine/errors.ts src/engine/moves.ts src/engine/applyMove.ts` shows every match ending in `.ts'`
    - `npm run build` exits 0
    - `npm test -- --run` (full suite) passes
    - `git diff --stat` shows no changes outside the five listed files
  </acceptance_criteria>
  <done>The engine's transitive module graph resolves under Deno's literal-specifier rules without duplicating any rule logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add toast variants for reconciliation and reconnect (D-10, D-12)</name>
  <files>src/hooks/useToast.ts, src/components/Toast.tsx, src/__tests__/hooks/useToast.test.ts, src/__tests__/components/Toast.test.tsx</files>
  <read_first>
    - src/hooks/useToast.ts (current `ToastState { message, code? }` and the two-arg `show(message, code?)`)
    - src/components/Toast.tsx (current single hardcoded `border-red-500` style, `role="alert"`, `aria-live="polite"`, `X` dismiss button)
    - src/__tests__/components/Toast.test.tsx and src/__tests__/hooks/useToast.test.ts (existing assertions that must keep passing)
    - .planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md (Color table: amber-500 for reconciliation, green-500 for reconnect; Copywriting Contract for icons and copy)
    - .planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md (the useToast/Toast section: the backward-compatible-default requirement)
  </read_first>
  <behavior>
    - `show(message)` with no code and no variant produces a toast whose `variant` is `'error'`
    - `show(message, code)` (the existing two-argument shape used by GameContext, GameScreen and MenuScreen) still compiles and still produces `variant: 'error'`
    - `show(message, code, 'reconcile')` produces `variant: 'reconcile'`
    - `Toast` renders `border-red-500` for `variant: 'error'`, `border-amber-500` for `'reconcile'`, `border-green-500` for `'reconnect'`
    - `Toast` still exposes `role="alert"` and a button with `aria-label="Dismiss"` for every variant
    - The 3500ms auto-dismiss timer behaves identically for all variants
  </behavior>
  <action>
In `src/hooks/useToast.ts`: add `variant?: 'error' | 'reconcile' | 'reconnect'` to `ToastState`
and export a `ToastVariant` type alias. Change `show`'s signature to
`show(message: string, code?: string, variant: ToastVariant = 'error')` — a defaulted third
parameter, so every existing two-argument call site keeps compiling unchanged. Do not change
`DISMISS_MS` (3500), the replace-not-queue behaviour, or the timer-reset behaviour.

In `src/components/Toast.tsx`: replace the hardcoded `border-red-500` with a lookup keyed on
`toast.variant ?? 'error'` mapping `error` → `border-red-500`, `reconcile` → `border-amber-500`,
`reconnect` → `border-green-500`. Add a leading icon from `lucide-react` per the UI-SPEC
Copywriting Contract: `RotateCcw` for `reconcile`, `CheckCircle2` for `reconnect`, and no icon
for `error` (preserving today's exact error appearance). Keep the `role="alert"`,
`aria-live="polite"`, `bg-slate-800`, `border-2`, `rounded-lg`, `p-4`, `fixed bottom-4 right-4`,
`z-50`, `max-w-sm` classes and the `X` dismiss button with `aria-label="Dismiss"` exactly as
they are — the existing `Toast.test.tsx` asserts on both and must not need editing to pass.

Extend the two existing test files with cases for the behaviours above. Do not create parallel
toast files or a second toast system.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/hooks/useToast.test.ts src/__tests__/components/Toast.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` (full suite) passes with zero edits to `src/context/GameContext.tsx`, `src/screens/GameScreen.tsx` or `src/screens/MenuScreen.tsx`
    - `grep -c 'border-amber-500' src/components/Toast.tsx` is at least 1
    - `grep -c 'border-green-500' src/components/Toast.tsx` is at least 1
    - `grep -c "role=\"alert\"" src/components/Toast.tsx` is 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Toasts can carry a reconciliation or reconnect style without any existing call site changing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied `Move` payload → `applyMove` | Every field is attacker-controlled once MPLAY-04 lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | `applyPickUpPile` `revealedFaceDownIndex` | mitigate | Gate on `getAvailableCardSource(player) === 'faceDown'`; reject with `INVALID_SELECTION` otherwise (Task 1), with three regression tests |
| T-02-04 | Elevation of Privilege | `applyPlayCards` play-order rule (CR-01) | mitigate | Already closed in commit `a8a4ef3`; Task 1 re-runs the two existing regression tests as a standing guard rather than re-implementing |
| T-02-05 | Tampering | Engine module graph | mitigate | Edge Functions import the same `src/engine/*` files verbatim (Task 2 enables this); no SQL/Deno reimplementation of any rule is permitted anywhere in this phase |
</threat_model>

<verification>
- `npm test -- --run` passes, including three new `PICK_UP_PILE` gate tests and the two untouched CR-01 tests
- `npm run build` passes with extension-qualified engine imports
- `grep -rn "revealedFaceDownIndex" src/engine/applyMove.ts` shows the gate above the bounds check
</verification>

<success_criteria>
- D-13's hardening intent satisfied: CR-01 verified closed, the `revealedFaceDownIndex` gap closed
- Engine modules resolvable by Deno without any duplicated rule logic
- Toast variants available for D-10/D-12 with no breaking call-site change
</success_criteria>

<output>
Create `.planning/phases/02-real-cross-device-multiplayer/02-02-SUMMARY.md` when done
</output>
