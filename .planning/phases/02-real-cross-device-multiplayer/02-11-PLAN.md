---
phase: 02-real-cross-device-multiplayer
plan: 11
type: execute
wave: 4
depends_on: ["02-06", "02-07", "02-09"]
files_modified:
  - src/screens/LobbyScreen.tsx
  - src/__tests__/screens/LobbyScreen.test.tsx
autonomous: true
requirements: [MPLAY-01, MPLAY-04, MPLAY-06]

must_haves:
  truths:
    - "The host starts the game and every player's cards are dealt by the server"
    - "A player can copy either the raw room code or a link that opens the game with the code already filled in"
    - "The host can remove a player who has dropped or wandered off, so the rest can start"
    - "If the host drops in the lobby, someone else becomes host and the room can still start"
    - "A player who is not currently connected is visibly marked in the lobby list"
  artifacts:
    - path: "src/screens/LobbyScreen.tsx"
      provides: "Server-backed start, join-link copy, host controls, offline markers"
      contains: "functions.invoke"
  key_links:
    - from: "src/screens/LobbyScreen.tsx"
      to: "start-game edge function"
      via: "functions.invoke"
      pattern: "functions\\.invoke\\('start-game'"
    - from: "src/screens/LobbyScreen.tsx"
      to: "remove-player edge function"
      via: "functions.invoke"
      pattern: "functions\\.invoke\\('remove-player'"
---

<objective>
Make the lobby work against the server: dealing moves to `start-game`, and the lobby-management
decisions D-07 (host removes an AFK player) and D-08 (host auto-transfer) get their UI. Add
D-15's join-link copy button alongside the existing room-code copy.

Purpose: MPLAY-01's sharing flow, MPLAY-04's "the client no longer deals", and MPLAY-06's
lobby-side disconnect handling.
Output: rewired `LobbyScreen.tsx` with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md
@.planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md
@src/supabase/roomTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Start the game through the server (MPLAY-04)</name>
  <files>src/screens/LobbyScreen.tsx, src/__tests__/screens/LobbyScreen.test.tsx</files>
  <read_first>
    - src/screens/LobbyScreen.tsx (the whole file — `startGame` lines 22-50 with its client-side `GameLogic.shuffleDeck(GameLogic.createDeck(numDecks))` deal and direct `setGameState` write, the `isHost` derivation at line 52, and the disabled/"Waiting for players..." button states at lines 103-115)
    - supabase/functions/_shared/startGame.ts (the request body and the `NOT_HOST` / `NOT_ENOUGH_PLAYERS` / `GAME_ALREADY_STARTED` errors it can return)
    - src/context/GameContext.tsx (`applyServerRoom`, `showToast`)
    - src/__tests__/screens/LobbyScreen.test.tsx (existing assertions that must keep passing)
  </read_first>
  <behavior>
    - Clicking Start Game invokes `start-game` with the room code and applies the returned room
    - The Start Game button stays disabled below 2 players and keeps its "Waiting for players..." label
    - A non-host sees the "Waiting for host to start the game..." text and no Start Game button, unchanged
    - A `NOT_ENOUGH_PLAYERS` or `NOT_HOST` error from the server surfaces as a toast rather than being swallowed
    - A transport failure surfaces the generic retry toast
    - `LobbyScreen` imports nothing from `src/gameLogic.ts` any more
  </behavior>
  <action>
Replace `startGame`'s body with
`getSupabaseClient().functions.invoke('start-game', { body: { roomCode } })`, applying the
returned room via context. Delete the client-side deal entirely — the `numDecks` calculation,
the `GameLogic.shuffleDeck(GameLogic.createDeck(...))` call, the per-player `deck.splice(0, 3)`
dealing and the `updatedState` literal all move server-side (plan 02-06). Remove the now-unused
`import * as GameLogic` and `import type { GameState }` lines; `tsconfig.app.json` has
`noUnusedLocals` on, so a leftover import fails the build.

Keep the client-side host and player-count guard as a UI affordance so the button behaves as it
does today, but treat the server's answer as the control: surface any returned `EdgeError` as a
toast rather than assuming the guard made it impossible.

Update `src/__tests__/screens/LobbyScreen.test.tsx` with the six behaviours above.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/screens/LobbyScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviours have a passing test
    - `grep -c 'gameLogic' src/screens/LobbyScreen.tsx` returns 0
    - `grep -c 'splice' src/screens/LobbyScreen.tsx` returns 0
    - `grep -c "functions.invoke('start-game'" src/screens/LobbyScreen.tsx` is 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dealing is entirely server-side and the lobby's start button reports real server errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copy join link alongside copy room code (D-15)</name>
  <files>src/screens/LobbyScreen.tsx, src/__tests__/screens/LobbyScreen.test.tsx</files>
  <read_first>
    - src/screens/LobbyScreen.tsx (`copyRoomCode` lines 15-20 and its `copied` state, and the button markup at lines 66-75 with the `Check`/`Copy` icon swap — the exact interaction to replicate)
    - src/App.tsx (the `/join/:code` path shape parsed in plan 02-10, so the generated link matches what the app can read back)
    - .planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md (Copywriting Contract: `Link2` to `Check` green-400 swap reverting after 2s, `aria-label="Copy join link"`, and backfill `aria-label="Copy room code"` on the existing button)
    - .planning/phases/02-real-cross-device-multiplayer/02-PATTERNS.md (the copy-to-clipboard 2s-revert pattern to replicate exactly, and the note about separate state so the two buttons do not fight over one boolean)
  </read_first>
  <behavior>
    - The join-link button writes `<origin>/join/<ROOMCODE>` to the clipboard
    - After clicking the join-link button its icon becomes `Check` and reverts after 2000ms
    - Clicking the join-link button does not change the room-code button's icon, and vice versa
    - The join-link button has `aria-label="Copy join link"` and the room-code button has `aria-label="Copy room code"`
    - Both buttons render nothing and do nothing when there is no game state
  </behavior>
  <action>
Add `copyJoinLink` next to `copyRoomCode`, replicating that function's shape exactly:
`navigator.clipboard.writeText(...)`, set a flag, `setTimeout(..., 2000)` to revert. Write
`` `${window.location.origin}/join/${gameState.roomCode}` `` — the path must match what
`App.tsx` parses, or the link silently does nothing on the recipient's device.

Give it its own `copiedLink` state separate from `copied`, so the two buttons do not share one
boolean and flicker each other.

Place it as a sibling button in the same room-code row, same `p-2 hover:bg-slate-700 rounded-lg
transition-colors` shape, using `Link2` in place of `Copy` and the same `Check` with
`text-green-400` on success. Add `aria-label="Copy join link"`, and while in this markup add
the missing `aria-label="Copy room code"` to the existing button — neither has one today.

Extend `src/__tests__/screens/LobbyScreen.test.tsx` with the five behaviours, stubbing
`navigator.clipboard.writeText` and using fake timers for the revert. Per CLAUDE.md's testing
note, assert the post-click icon state immediately after `fireEvent.click` rather than inside
`waitFor`, so a real-timer race cannot false-negative.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/screens/LobbyScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All five behaviours have a passing test, including the independent-state case
    - `grep -c 'aria-label="Copy join link"' src/screens/LobbyScreen.tsx` is 1
    - `grep -c 'aria-label="Copy room code"' src/screens/LobbyScreen.tsx` is 1
    - `grep -c '/join/' src/screens/LobbyScreen.tsx` is at least 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>A player can share either a code or a one-click link, with the existing copy interaction unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lobby offline markers, host removal and host transfer display (D-07, D-08, D-10)</name>
  <files>src/screens/LobbyScreen.tsx, src/__tests__/screens/LobbyScreen.test.tsx</files>
  <read_first>
    - src/screens/LobbyScreen.tsx (the player-list rows at lines 87-100 — the `Crown` host icon, the `text-xs bg-purple-600 px-2 py-1 rounded` "You" badge that sets this project's badge shape, and the row container classes)
    - src/hooks/usePresence.ts (`isPlayerOffline` — the display signal)
    - supabase/functions/_shared/removePlayer.ts (the request body and the `NOT_HOST` / `GAME_ALREADY_STARTED` / `BAD_REQUEST` errors)
    - .planning/phases/02-real-cross-device-multiplayer/02-UI-SPEC.md (Color table: `slate-500`/`slate-600` neutral-offline, `opacity-60` on the tile; the 8px `text-xs px-2 py-1 rounded` badge pattern)
    - .planning/phases/02-real-cross-device-multiplayer/02-CONTEXT.md (D-07 and D-08)
  </read_first>
  <behavior>
    - A player reported offline by `isPlayerOffline` renders with `opacity-60` and an "Offline" badge; a connected player renders unchanged
    - A remove button renders only for the host, and only on rows that are neither the host's own row nor a connected player's row is it enabled — it is present but disabled for connected players
    - Clicking remove invokes `remove-player` with the room code and the target's id and applies the returned room
    - A non-host sees no remove buttons at all
    - When the host changes because the server transferred it, the `Crown` icon and the Start Game button follow `state.host` with no extra client logic
    - An `EdgeError` from `remove-player` surfaces as a toast
  </behavior>
  <action>
Take `isPlayerOffline` from the presence hook (threaded down in plan 02-10) and use it to add
two things to each player row: `opacity-60` plus a `border-slate-600` treatment on the row
container when offline, and an "Offline" badge using the exact existing badge shape
(`text-xs px-2 py-1 rounded`, the same one the "You" badge uses) in the neutral slate palette
with a `WifiOff` icon. Follow the conditional-class-string style already used in this file
rather than introducing a class-name helper.

Add D-07's remove control: a small icon button on each non-host row, rendered only when the
viewer is the host, invoking `remove-player`. Render it disabled rather than hidden for
currently-connected players so the host can see the control exists and why it is unavailable —
the server independently allows removing any non-host lobby player, so this disabled state is
purely a "are you sure you meant this" affordance, not a security control.

D-08 needs no client work beyond making sure nothing caches the host: `isHost` must stay derived
from `gameState.host === playerId` on every render, so when the server transfers host the
`Crown` icon and the Start Game button move automatically on the next Realtime payload. Confirm
there is no memoised host value; if one exists, remove it.

Extend `src/__tests__/screens/LobbyScreen.test.tsx` with the six behaviours, including a case
that re-renders with a changed `state.host` and asserts the Start Game button appears for the
new host.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/screens/LobbyScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviours have a passing test, including the host-transfer re-render case
    - `grep -c 'opacity-60' src/screens/LobbyScreen.tsx` is at least 1
    - `grep -c "functions.invoke('remove-player'" src/screens/LobbyScreen.tsx` is 1
    - `grep -c 'useMemo' src/screens/LobbyScreen.tsx` returns 0 for any host derivation
    - `npm test -- --run` (full suite) passes
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>The lobby shows who has dropped, lets the host clear them out, and follows a server-side host transfer automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lobby UI → `start-game` / `remove-player` | Any client can call these regardless of what the UI shows |
| clipboard → other people | The join link is shared outside the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-38 | Elevation of Privilege | Hidden/disabled host controls treated as security | mitigate | The UI gating is explicitly an affordance; `startGame` and `removePlayer` each re-check `caller === state.host` and the lobby phase server-side (plans 02-06, 02-07), and their error responses are surfaced rather than assumed impossible |
| T-02-39 | Tampering | Client-dealt deck | mitigate | The entire client-side deal is deleted; an acceptance criterion greps for zero `gameLogic` imports in this file so it cannot creep back |
| T-02-40 | Information Disclosure | Shared join link leaking a room | accept | A join link conveys exactly what a shared room code conveys, which is the intended sharing mechanism; the room contains only card positions in a party game (same acceptance as T-02-08) |
</threat_model>

<verification>
- `npm test -- --run` passes
- `grep -rn "gameLogic" src/screens/LobbyScreen.tsx` returns nothing
- `npm run build` and `npm run lint` clean
</verification>

<success_criteria>
- Server deals; client no longer can
- Both sharing affordances present and independently behaved
- D-07 and D-08 have working UI backed by server-side checks
</success_criteria>

<output>
Create `.planning/phases/02-real-cross-device-multiplayer/02-11-SUMMARY.md` when done
</output>
