# Decisions (from ADRs)

No ADR-type documents were present in this ingest. The source document
(`ROADMAP.md`) contains a "Decisions made" section, but it is classified as
type `DOC` (not `ADR`), so its content is captured as context rather than as
locked/proposed architectural decisions — see `context.md` under the
"Decisions made (as stated in ROADMAP.md)" topic.

No entries in this bucket for the current ingest.
